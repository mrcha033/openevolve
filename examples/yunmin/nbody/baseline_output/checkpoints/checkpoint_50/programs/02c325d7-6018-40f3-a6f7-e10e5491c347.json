{"id": "02c325d7-6018-40f3-a6f7-e10e5491c347", "code": "#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\n// Array-of-Structures layout (deliberately naive for cache performance)\nstruct Body {\n    double x, y, z;\n    double vx, vy, vz;\n    double mass;\n};\n\n// Reference implementation (outside EVOLVE-BLOCK, used for correctness check)\nstatic void ReferenceForces(const std::vector<Body> &bodies,\n                            std::vector<double> &fx,\n                            std::vector<double> &fy,\n                            std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    for (int i = 0; i < n; ++i) {\n        for (int j = i + 1; j < n; ++j) {\n            double dx = bodies[j].x - bodies[i].x;\n            double dy = bodies[j].y - bodies[i].y;\n            double dz = bodies[j].z - bodies[i].z;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            double fi = bodies[j].mass * inv_r3;\n            double fj = bodies[i].mass * inv_r3;\n            fx[i] += dx * fi; fy[i] += dy * fi; fz[i] += dz * fi;\n            fx[j] -= dx * fj; fy[j] -= dy * fj; fz[j] -= dz * fj;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void ComputeForces(const std::vector<Body> &bodies,\n                          std::vector<double> &fx,\n                          std::vector<double> &fy,\n                          std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    \n    // Highly optimized version with:\n    // 1. Loop unrolling to reduce loop overhead\n    // 2. Pre-computed values to minimize redundant calculations\n    // 3. Better instruction scheduling for modern CPUs\n    // 4. Reduced memory access patterns\n    // 5. Single sqrt call per pair for accuracy\n    \n    for (int i = 0; i < n; ++i) {\n        const Body& bi = bodies[i];\n        const double xi = bi.x;\n        const double yi = bi.y;\n        const double zi = bi.z;\n        const double mi = bi.mass;\n        \n        // Process 4 bodies at a time to reduce loop overhead\n        int j = i + 1;\n        for (; j <= n - 4; j += 4) {\n            // Process four pairs simultaneously\n            const Body& bj0 = bodies[j];\n            const Body& bj1 = bodies[j+1];\n            const Body& bj2 = bodies[j+2];\n            const Body& bj3 = bodies[j+3];\n            \n            // First pair (i, j)\n            double dx0 = bj0.x - xi;\n            double dy0 = bj0.y - yi;\n            double dz0 = bj0.z - zi;\n            double r2_0 = dx0 * dx0 + dy0 * dy0 + dz0 * dz0 + eps2;\n            double inv_r0 = 1.0 / std::sqrt(r2_0);\n            double inv_r3_0 = inv_r0 * inv_r0 * inv_r0;\n            double force_factor_0 = bj0.mass * inv_r3_0;\n            \n            fx[i] += dx0 * force_factor_0;\n            fy[i] += dy0 * force_factor_0;\n            fz[i] += dz0 * force_factor_0;\n            \n            fx[j] -= dx0 * force_factor_0;\n            fy[j] -= dy0 * force_factor_0;\n            fz[j] -= dz0 * force_factor_0;\n            \n            // Second pair (i, j+1)\n            double dx1 = bj1.x - xi;\n            double dy1 = bj1.y - yi;\n            double dz1 = bj1.z - zi;\n            double r2_1 = dx1 * dx1 + dy1 * dy1 + dz1 * dz1 + eps2;\n            double inv_r1 = 1.0 / std::sqrt(r2_1);\n            double inv_r3_1 = inv_r1 * inv_r1 * inv_r1;\n            double force_factor_1 = bj1.mass * inv_r3_1;\n            \n            fx[i] += dx1 * force_factor_1;\n            fy[i] += dy1 * force_factor_1;\n            fz[i] += dz1 * force_factor_1;\n            \n            fx[j+1] -= dx1 * force_factor_1;\n            fy[j+1] -= dy1 * force_factor_1;\n            fz[j+1] -= dz1 * force_factor_1;\n            \n            // Third pair (i, j+2)\n            double dx2 = bj2.x - xi;\n            double dy2 = bj2.y - yi;\n            double dz2 = bj2.z - zi;\n            double r2_2 = dx2 * dx2 + dy2 * dy2 + dz2 * dz2 + eps2;\n            double inv_r2 = 1.0 / std::sqrt(r2_2);\n            double inv_r3_2 = inv_r2 * inv_r2 * inv_r3_2;\n            double force_factor_2 = bj2.mass * inv_r3_2;\n            \n            fx[i] += dx2 * force_factor_2;\n            fy[i] += dy2 * force_factor_2;\n            fz[i] += dz2 * force_factor_2;\n            \n            fx[j+2] -= dx2 * force_factor_2;\n            fy[j+2] -= dy2 * force_factor_2;\n            fz[j+2] -= dz2 * force_factor_2;\n            \n            // Fourth pair (i, j+3)\n            double dx3 = bj3.x - xi;\n            double dy3 = bj3.y - yi;\n            double dz3 = bj3.z - zi;\n            double r2_3 = dx3 * dx3 + dy3 * dy3 + dz3 * dz3 + eps2;\n            double inv_r3 = 1.0 / std::sqrt(r2_3);\n            double inv_r3_3 = inv_r3 * inv_r3 * inv_r3;\n            double force_factor_3 = bj3.mass * inv_r3_3;\n            \n            fx[i] += dx3 * force_factor_3;\n            fy[i] += dy3 * force_factor_3;\n            fz[i] += dz3 * force_factor_3;\n            \n            fx[j+3] -= dx3 * force_factor_3;\n            fy[j+3] -= dy3 * force_factor_3;\n            fz[j+3] -= dz3 * force_factor_3;\n        }\n        \n        // Handle remaining pairs\n        for (; j < n; ++j) {\n            const Body& bj = bodies[j];\n            double dx = bj.x - xi;\n            double dy = bj.y - yi;\n            double dz = bj.z - zi;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            double force_factor = bj.mass * inv_r3;\n            \n            fx[i] += dx * force_factor;\n            fy[i] += dy * force_factor;\n            fz[i] += dz * force_factor;\n            \n            fx[j] -= dx * force_factor;\n            fy[j] -= dy * force_factor;\n            fz[j] -= dz * force_factor;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic std::vector<Body> GenerateBodies(std::mt19937 &rng, int n) {\n    std::uniform_real_distribution<double> pos(-100.0, 100.0);\n    std::uniform_real_distribution<double> vel(-1.0, 1.0);\n    std::uniform_real_distribution<double> m(0.1, 10.0);\n    std::vector<Body> bodies(n);\n    for (int i = 0; i < n; ++i) {\n        bodies[i] = {pos(rng), pos(rng), pos(rng),\n                     vel(rng), vel(rng), vel(rng), m(rng)};\n    }\n    return bodies;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int num_bodies = 1024;\n    int rounds = 20;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--bodies\" && i + 1 < argc) num_bodies = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    std::mt19937 rng(seed);\n    auto bodies = GenerateBodies(rng, num_bodies);\n\n    // Correctness check\n    std::vector<double> ref_fx, ref_fy, ref_fz;\n    std::vector<double> fx, fy, fz;\n    ReferenceForces(bodies, ref_fx, ref_fy, ref_fz);\n    ComputeForces(bodies, fx, fy, fz);\n    for (int i = 0; i < num_bodies; ++i) {\n        double mag = std::abs(ref_fx[i]) + std::abs(ref_fy[i]) + std::abs(ref_fz[i]);\n        double err = std::abs(fx[i] - ref_fx[i]) + std::abs(fy[i] - ref_fy[i])\n                   + std::abs(fz[i] - ref_fz[i]);\n        // Relative tolerance: allow FP reordering from SIMD/SoA/tiling\n        double tol = std::max(1e-6, mag * 1e-6);\n        if (err > tol) {\n            std::cerr << \"force mismatch at body \" << i\n                      << \" err=\" << err << \" tol=\" << tol << std::endl;\n            return 2;\n        }\n    }\n\n    // Benchmark\n    std::vector<double> latencies;\n    latencies.reserve(rounds);\n    double total_time = 0.0;\n\n    for (int r = 0; r < rounds; ++r) {\n        auto t0 = std::chrono::high_resolution_clock::now();\n        ComputeForces(bodies, fx, fy, fz);\n        auto t1 = std::chrono::high_resolution_clock::now();\n        double dt = std::chrono::duration<double>(t1 - t0).count();\n        latencies.push_back(dt);\n        total_time += dt;\n    }\n\n    double ops_per_sec = rounds / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"num_bodies\\\":\" << num_bodies << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}", "changes_description": null, "language": "cpp", "parent_id": "5c7335d4-ea26-4fe0-b794-9cc7d021dbdc", "generation": 3, "timestamp": 1770565575.6987464, "iteration_found": 34, "metrics": {"combined_score": 0.0, "error": "nbody failed: force mismatch at body 0 err=0.287437 tol=1e-06", "ops_per_sec": 0.0, "p99_latency_us": 0.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"combined_score": 0.0, "error": "nbody failed: force mismatch at body 1 err=0.000640097 tol=1e-06", "ops_per_sec": 0.0, "p99_latency_us": 0.0}, "island": 1}, "prompts": {"full_rewrite_user": {"system": "You are a C++ performance optimization expert.\nOptimize the N-body force computation inside the EVOLVE-BLOCK.\n\nRequirements:\n- Only modify code inside the EVOLVE-BLOCK.\n- Keep the function signature: void ComputeForces(const vector<Body>&, vector<double>&, vector<double>&, vector<double>&)\n- The Body struct has fields: x, y, z, vx, vy, vz, mass.\n- Must compute gravitationally correct pairwise forces with softening eps2 = 1e-9.\n- Results must match the reference within 1e-6 relative error per body.\n- Use only the C++ standard library. No external dependencies.\n- Do not add top-level side effects or debug printing.\n\nMake the force computation as fast as possible.\n", "user": "# Current Program Information\n- Fitness: 0.0000\n- Feature coordinates: ops_per_sec=0.00, p99_latency_us=0.00\n- Focus areas: - Fitness unchanged at 0.0000\n- Exploring ops_per_sec=0.00, p99_latency_us=0.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: combined_score: 0.0000, error: nbody failed: force mismatch at body 1 err=0.000640097 tol=1e-06, ops_per_sec: 0.0000, p99_latency_us: 0.0000\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: combined_score: 0.0000, error: nbody failed: force mismatch at body 1 err=0.000640097 tol=1e-06, ops_per_sec: 0.0000, p99_latency_us: 0.0000\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: combined_score: 1.4245, ops_per_sec: 363.6347, p99_latency_us: 2798.5250, bcoz_max_speedup: 5.0000, bperf_offcpu_ratio: 0.0200\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 1.4245)\n```cpp\n#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\n// Array-of-Structures layout (deliberately naive for cache performance)\nstruct Body {\n    double x, y, z;\n    double vx, vy, vz;\n    double mass;\n};\n\n// Reference implementation (outside EVOLVE-BLOCK, used for correctness check)\nstatic void ReferenceForces(const std::vector<Body> &bodies,\n                            std::vector<double> &fx,\n                            std::vector<double> &fy,\n                            std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    for (int i = 0; i < n; ++i) {\n        for (int j = i + 1; j < n; ++j) {\n            double dx = bodies[j].x - bodies[i].x;\n            double dy = bodies[j].y - bodies[i].y;\n            double dz = bodies[j].z - bodies[i].z;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            double fi = bodies[j].mass * inv_r3;\n            double fj = bodies[i].mass * inv_r3;\n            fx[i] += dx * fi; fy[i] += dy * fi; fz[i] += dz * fi;\n            fx[j] -= dx * fj; fy[j] -= dy * fj; fz[j] -= dz * fj;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void ComputeForces(const std::vector<Body> &bodies,\n                          std::vector<double> &fx,\n                          std::vector<double> &fy,\n                          std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    \n    // Loop unrolling and manual vectorization for better performance\n    for (int i = 0; i < n; ++i) {\n        const Body& bi = bodies[i];\n        for (int j = i + 1; j < n; ++j) {\n            const Body& bj = bodies[j];\n            double dx = bj.x - bi.x;\n            double dy = bj.y - bi.y;\n            double dz = bj.z - bi.z;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            \n            // Use fast reciprocal square root approximation\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            \n            double fi = bj.mass * inv_r3;\n            double fj = bi.mass * inv_r3;\n            \n            fx[i] += dx * fi;\n            fy[i] += dy * fi;\n            fz[i] += dz * fi;\n            \n            fx[j] -= dx * fj;\n            fy[j] -= dy * fj;\n            fz[j] -= dz * fj;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic std::vector<Body> GenerateBodies(std::mt19937 &rng, int n) {\n    std::uniform_real_distribution<double> pos(-100.0, 100.0);\n    std::uniform_real_distribution<double> vel(-1.0, 1.0);\n    std::uniform_real_distribution<double> m(0.1, 10.0);\n    std::vector<Body> bodies(n);\n    for (int i = 0; i < n; ++i) {\n        bodies[i] = {pos(rng), pos(rng), pos(rng),\n                     vel(rng), vel(rng), vel(rng), m(rng)};\n    }\n    return bodies;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int num_bodies = 1024;\n    int rounds = 20;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--bodies\" && i + 1 < argc) num_bodies = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    std::mt19937 rng(seed);\n    auto bodies = GenerateBodies(rng, num_bodies);\n\n    // Correctness check\n    std::vector<double> ref_fx, ref_fy, ref_fz;\n    std::vector<double> fx, fy, fz;\n    ReferenceForces(bodies, ref_fx, ref_fy, ref_fz);\n    ComputeForces(bodies, fx, fy, fz);\n    for (int i = 0; i < num_bodies; ++i) {\n        double mag = std::abs(ref_fx[i]) + std::abs(ref_fy[i]) + std::abs(ref_fz[i]);\n        double err = std::abs(fx[i] - ref_fx[i]) + std::abs(fy[i] - ref_fy[i])\n                   + std::abs(fz[i] - ref_fz[i]);\n        // Relative tolerance: allow FP reordering from SIMD/SoA/tiling\n        double tol = std::max(1e-6, mag * 1e-6);\n        if (err > tol) {\n            std::cerr << \"force mismatch at body \" << i\n                      << \" err=\" << err << \" tol=\" << tol << std::endl;\n            return 2;\n        }\n    }\n\n    // Benchmark\n    std::vector<double> latencies;\n    latencies.reserve(rounds);\n    double total_time = 0.0;\n\n    for (int r = 0; r < rounds; ++r) {\n        auto t0 = std::chrono::high_resolution_clock::now();\n        ComputeForces(bodies, fx, fy, fz);\n        auto t1 = std::chrono::high_resolution_clock::now();\n        double dt = std::chrono::duration<double>(t1 - t0).count();\n        latencies.push_back(dt);\n        total_time += dt;\n    }\n\n    double ops_per_sec = rounds / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"num_bodies\\\":\" << num_bodies << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}\n```\nKey features: Performs well on combined_score (1.4245), Performs well on ops_per_sec (363.6347), Performs well on p99_latency_us (2798.5250), Performs well on bcoz_max_speedup (5.0000), Performs well on bperf_offcpu_ratio (0.0200)\n\n### Program 2 (Score: 0.0000)\n```cpp\n#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\n// Array-of-Structures layout (deliberately naive for cache performance)\nstruct Body {\n    double x, y, z;\n    double vx, vy, vz;\n    double mass;\n};\n\n// Reference implementation (outside EVOLVE-BLOCK, used for correctness check)\nstatic void ReferenceForces(const std::vector<Body> &bodies,\n                            std::vector<double> &fx,\n                            std::vector<double> &fy,\n                            std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    for (int i = 0; i < n; ++i) {\n        for (int j = i + 1; j < n; ++j) {\n            double dx = bodies[j].x - bodies[i].x;\n            double dy = bodies[j].y - bodies[i].y;\n            double dz = bodies[j].z - bodies[i].z;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            double fi = bodies[j].mass * inv_r3;\n            double fj = bodies[i].mass * inv_r3;\n            fx[i] += dx * fi; fy[i] += dy * fi; fz[i] += dz * fi;\n            fx[j] -= dx * fj; fy[j] -= dy * fj; fz[j] -= dz * fj;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void ComputeForces(const std::vector<Body> &bodies,\n                          std::vector<double> &fx,\n                          std::vector<double> &fy,\n                          std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    \n    // Optimized version with:\n    // 1. Loop unrolling to reduce loop overhead\n    // 2. Pre-computed values to minimize redundant calculations\n    // 3. Better instruction scheduling for modern CPUs\n    // 4. Reduced memory access patterns\n    \n    for (int i = 0; i < n; ++i) {\n        const Body& bi = bodies[i];\n        const double xi = bi.x;\n        const double yi = bi.y;\n        const double zi = bi.z;\n        const double mi = bi.mass;\n        \n        // Process 4 bodies at a time to reduce loop overhead\n        int j = i + 1;\n        for (; j <= n - 4; j += 4) {\n            // Process four pairs simultaneously\n            const Body& bj0 = bodies[j];\n            const Body& bj1 = bodies[j+1];\n            const Body& bj2 = bodies[j+2];\n            const Body& bj3 = bodies[j+3];\n            \n            // First pair (i, j)\n            double dx0 = bj0.x - xi;\n            double dy0 = bj0.y - yi;\n            double dz0 = bj0.z - zi;\n            double r2_0 = dx0 * dx0 + dy0 * dy0 + dz0 * dz0 + eps2;\n            double inv_r0 = 1.0 / std::sqrt(r2_0);\n            double inv_r3_0 = inv_r0 * inv_r0 * inv_r0;\n            double force_factor_0 = bj0.mass * inv_r3_0;\n            \n            fx[i] += dx0 * force_factor_0;\n            fy[i] += dy0 * force_factor_0;\n            fz[i] += dz0 * force_factor_0;\n            \n            fx[j] -= dx0 * force_factor_0;\n            fy[j] -= dy0 * force_factor_0;\n            fz[j] -= dz0 * force_factor_0;\n            \n            // Second pair (i, j+1)\n            double dx1 = bj1.x - xi;\n            double dy1 = bj1.y - yi;\n            double dz1 = bj1.z - zi;\n            double r2_1 = dx1 * dx1 + dy1 * dy1 + dz1 * dz1 + eps2;\n            double inv_r1 = 1.0 / std::sqrt(r2_1);\n            double inv_r3_1 = inv_r1 * inv_r1 * inv_r1;\n            double force_factor_1 = bj1.mass * inv_r3_1;\n            \n            fx[i] += dx1 * force_factor_1;\n            fy[i] += dy1 * force_factor_1;\n            fz[i] += dz1 * force_factor_1;\n            \n            fx[j+1] -= dx1 * force_factor_1;\n            fy[j+1] -= dy1 * force_factor_1;\n            fz[j+1] -= dz1 * force_factor_1;\n            \n            // Third pair (i, j+2)\n            double dx2 = bj2.x - xi;\n            double dy2 = bj2.y - yi;\n            double dz2 = bj2.z - zi;\n            double r2_2 = dx2 * dx2 + dy2 * dy2 + dz2 * dz2 + eps2;\n            double inv_r2 = 1.0 / std::sqrt(r2_2);\n            double inv_r3_2 = inv_r2 * inv_r2 * inv_r2;\n            double force_factor_2 = bj2.mass * inv_r3_2;\n            \n            fx[i] += dx2 * force_factor_2;\n            fy[i] += dy2 * force_factor_2;\n            fz[i] += dz2 * force_factor_2;\n            \n            fx[j+2] -= dx2 * force_factor_2;\n            fy[j+2] -= dy2 * force_factor_2;\n            fz[j+2] -= dz2 * force_factor_2;\n            \n            // Fourth pair (i, j+3)\n            double dx3 = bj3.x - xi;\n            double dy3 = bj3.y - yi;\n            double dz3 = bj3.z - zi;\n            double r2_3 = dx3 * dx3 + dy3 * dy3 + dz3 * dz3 + eps2;\n            double inv_r3 = 1.0 / std::sqrt(r2_3);\n            double inv_r3_3 = inv_r3 * inv_r3 * inv_r3;\n            double force_factor_3 = bj3.mass * inv_r3_3;\n            \n            fx[i] += dx3 * force_factor_3;\n            fy[i] += dy3 * force_factor_3;\n            fz[i] += dz3 * force_factor_3;\n            \n            fx[j+3] -= dx3 * force_factor_3;\n            fy[j+3] -= dy3 * force_factor_3;\n            fz[j+3] -= dz3 * force_factor_3;\n        }\n        \n        // Handle remaining pairs\n        for (; j < n; ++j) {\n            const Body& bj = bodies[j];\n            double dx = bj.x - xi;\n            double dy = bj.y - yi;\n            double dz = bj.z - zi;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            double force_factor = bj.mass * inv_r3;\n            \n            fx[i] += dx * force_factor;\n            fy[i] += dy * force_factor;\n            fz[i] += dz * force_factor;\n            \n            fx[j] -= dx * force_factor;\n            fy[j] -= dy * force_factor;\n            fz[j] -= dz * force_factor;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic std::vector<Body> GenerateBodies(std::mt19937 &rng, int n) {\n    std::uniform_real_distribution<double> pos(-100.0, 100.0);\n    std::uniform_real_distribution<double> vel(-1.0, 1.0);\n    std::uniform_real_distribution<double> m(0.1, 10.0);\n    std::vector<Body> bodies(n);\n    for (int i = 0; i < n; ++i) {\n        bodies[i] = {pos(rng), pos(rng), pos(rng),\n                     vel(rng), vel(rng), vel(rng), m(rng)};\n    }\n    return bodies;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int num_bodies = 1024;\n    int rounds = 20;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--bodies\" && i + 1 < argc) num_bodies = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    std::mt19937 rng(seed);\n    auto bodies = GenerateBodies(rng, num_bodies);\n\n    // Correctness check\n    std::vector<double> ref_fx, ref_fy, ref_fz;\n    std::vector<double> fx, fy, fz;\n    ReferenceForces(bodies, ref_fx, ref_fy, ref_fz);\n    ComputeForces(bodies, fx, fy, fz);\n    for (int i = 0; i < num_bodies; ++i) {\n        double mag = std::abs(ref_fx[i]) + std::abs(ref_fy[i]) + std::abs(ref_fz[i]);\n        double err = std::abs(fx[i] - ref_fx[i]) + std::abs(fy[i] - ref_fy[i])\n                   + std::abs(fz[i] - ref_fz[i]);\n        // Relative tolerance: allow FP reordering from SIMD/SoA/tiling\n        double tol = std::max(1e-6, mag * 1e-6);\n        if (err > tol) {\n            std::cerr << \"force mismatch at body \" << i\n                      << \" err=\" << err << \" tol=\" << tol << std::endl;\n            return 2;\n        }\n    }\n\n    // Benchmark\n    std::vector<double> latencies;\n    latencies.reserve(rounds);\n    double total_time = 0.0;\n\n    for (int r = 0; r < rounds; ++r) {\n        auto t0 = std::chrono::high_resolution_clock::now();\n        ComputeForces(bodies, fx, fy, fz);\n        auto t1 = std::chrono::high_resolution_clock::now();\n        double dt = std::chrono::duration<double>(t1 - t0).count();\n        latencies.push_back(dt);\n        total_time += dt;\n    }\n\n    double ops_per_sec = rounds / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"num_bodies\\\":\" << num_bodies << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}\n```\nKey features: Performs well on combined_score (0.0000), Performs well on error (nbody failed: force mismatch at body 1 err=0.000640097 tol=1e-06), Performs well on ops_per_sec (0.0000), Performs well on p99_latency_us (0.0000)\n\n### Program 3 (Score: 0.0000)\n```cpp\n#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\n// Array-of-Structures layout (deliberately naive for cache performance)\nstruct Body {\n    double x, y, z;\n    double vx, vy, vz;\n    double mass;\n};\n\n// Reference implementation (outside EVOLVE-BLOCK, used for correctness check)\nstatic void ReferenceForces(const std::vector<Body> &bodies,\n                            std::vector<double> &fx,\n                            std::vector<double> &fy,\n                            std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    for (int i = 0; i < n; ++i) {\n        for (int j = i + 1; j < n; ++j) {\n            double dx = bodies[j].x - bodies[i].x;\n            double dy = bodies[j].y - bodies[i].y;\n            double dz = bodies[j].z - bodies[i].z;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            double fi = bodies[j].mass * inv_r3;\n            double fj = bodies[i].mass * inv_r3;\n            fx[i] += dx * fi; fy[i] += dy * fi; fz[i] += dz * fi;\n            fx[j] -= dx * fj; fy[j] -= dy * fj; fz[j] -= dz * fj;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void ComputeForces(const std::vector<Body> &bodies,\n                          std::vector<double> &fx,\n                          std::vector<double> &fy,\n                          std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    \n    // Highly optimized version with:\n    // 1. Precomputed constants and local variables\n    // 2. Reduced redundant operations\n    // 3. Better instruction scheduling\n    // 4. Minimized memory access patterns\n    // 5. Single sqrt call per pair for accuracy\n    \n    for (int i = 0; i < n; ++i) {\n        const Body& bi = bodies[i];\n        const double xi = bi.x;\n        const double yi = bi.y;\n        const double zi = bi.z;\n        const double mi = bi.mass;\n        \n        for (int j = i + 1; j < n; ++j) {\n            const Body& bj = bodies[j];\n            double dx = bj.x - xi;\n            double dy = bj.y - yi;\n            double dz = bj.z - zi;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            \n            // Compute inverse square root once with single sqrt call\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            \n            // Compute force factor once to avoid duplicate computation\n            double force_factor = bj.mass * inv_r3;\n            \n            // Apply forces to both bodies with minimal redundant operations\n            fx[i] += dx * force_factor;\n            fy[i] += dy * force_factor;\n            fz[i] += dz * force_factor;\n            \n            fx[j] -= dx * force_factor;\n            fy[j] -= dy * force_factor;\n            fz[j] -= dz * force_factor;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic std::vector<Body> GenerateBodies(std::mt19937 &rng, int n) {\n    std::uniform_real_distribution<double> pos(-100.0, 100.0);\n    std::uniform_real_distribution<double> vel(-1.0, 1.0);\n    std::uniform_real_distribution<double> m(0.1, 10.0);\n    std::vector<Body> bodies(n);\n    for (int i = 0; i < n; ++i) {\n        bodies[i] = {pos(rng), pos(rng), pos(rng),\n                     vel(rng), vel(rng), vel(rng), m(rng)};\n    }\n    return bodies;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int num_bodies = 1024;\n    int rounds = 20;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--bodies\" && i + 1 < argc) num_bodies = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    std::mt19937 rng(seed);\n    auto bodies = GenerateBodies(rng, num_bodies);\n\n    // Correctness check\n    std::vector<double> ref_fx, ref_fy, ref_fz;\n    std::vector<double> fx, fy, fz;\n    ReferenceForces(bodies, ref_fx, ref_fy, ref_fz);\n    ComputeForces(bodies, fx, fy, fz);\n    for (int i = 0; i < num_bodies; ++i) {\n        double mag = std::abs(ref_fx[i]) + std::abs(ref_fy[i]) + std::abs(ref_fz[i]);\n        double err = std::abs(fx[i] - ref_fx[i]) + std::abs(fy[i] - ref_fy[i])\n                   + std::abs(fz[i] - ref_fz[i]);\n        // Relative tolerance: allow FP reordering from SIMD/SoA/tiling\n        double tol = std::max(1e-6, mag * 1e-6);\n        if (err > tol) {\n            std::cerr << \"force mismatch at body \" << i\n                      << \" err=\" << err << \" tol=\" << tol << std::endl;\n            return 2;\n        }\n    }\n\n    // Benchmark\n    std::vector<double> latencies;\n    latencies.reserve(rounds);\n    double total_time = 0.0;\n\n    for (int r = 0; r < rounds; ++r) {\n        auto t0 = std::chrono::high_resolution_clock::now();\n        ComputeForces(bodies, fx, fy, fz);\n        auto t1 = std::chrono::high_resolution_clock::now();\n        double dt = std::chrono::duration<double>(t1 - t0).count();\n        latencies.push_back(dt);\n        total_time += dt;\n    }\n\n    double ops_per_sec = rounds / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"num_bodies\\\":\" << num_bodies << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}\n```\nKey features: Performs well on combined_score (0.0000), Performs well on error (nbody failed: force mismatch at body 1 err=0.000640097 tol=1e-06), Performs well on ops_per_sec (0.0000), Performs well on p99_latency_us (0.0000)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.0000)\n```cpp\n#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\n// Array-of-Structures layout (deliberately naive for cache performance)\nstruct Body {\n    double x, y, z;\n    double vx, vy, vz;\n    double mass;\n};\n\n// Reference implementation (outside EVOLVE-BLOCK, used for correctness check)\nstatic void ReferenceForces(const std::vector<Body> &bodies,\n                            std::vector<double> &fx,\n                            std::vector<double> &fy,\n                            std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    for (int i = 0; i < n; ++i) {\n        for (int j = i + 1; j < n; ++j) {\n            double dx = bodies[j].x - bodies[i].x;\n            double dy = bodies[j].y - bodies[i].y;\n            double dz = bodies[j].z - bodies[i].z;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            double fi = bodies[j].mass * inv_r3;\n            double fj = bodies[i].mass * inv_r3;\n            fx[i] += dx * fi; fy[i] += dy * fi; fz[i] += dz * fi;\n            fx[j] -= dx * fj; fy[j] -= dy * fj; fz[j] -= dz * fj;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void ComputeForces(const std::vector<Body> &bodies,\n                          std::vector<double> &fx,\n                          std::vector<double> &fy,\n                          std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    \n    // Optimized version with:\n    // 1. Precomputed constants\n    // 2. Reduced redundant operations\n    // 3. Better instruction scheduling\n    // 4. Avoiding repeated member access\n    \n    for (int i = 0; i < n; ++i) {\n        const Body& bi = bodies[i];\n        const double xi = bi.x;\n        const double yi = bi.y;\n        const double zi = bi.z;\n        const double mi = bi.mass;\n        \n        for (int j = i + 1; j < n; ++j) {\n            const Body& bj = bodies[j];\n            double dx = bj.x - xi;\n            double dy = bj.y - yi;\n            double dz = bj.z - zi;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            \n            // Compute inverse square root once\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            \n            // Compute force factor once\n            double force_factor = bj.mass * inv_r3;\n            \n            // Apply forces to both bodies\n            fx[i] += dx * force_factor;\n            fy[i] += dy * force_factor;\n            fz[i] += dz * force_factor;\n            \n            fx[j] -= dx * force_factor;\n            fy[j] -= dy * force_factor;\n            fz[j] -= dz * force_factor;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic std::vector<Body> GenerateBodies(std::mt19937 &rng, int n) {\n    std::uniform_real_distribution<double> pos(-100.0, 100.0);\n    std::uniform_real_distribution<double> vel(-1.0, 1.0);\n    std::uniform_real_distribution<double> m(0.1, 10.0);\n    std::vector<Body> bodies(n);\n    for (int i = 0; i < n; ++i) {\n        bodies[i] = {pos(rng), pos(rng), pos(rng),\n                     vel(rng), vel(rng), vel(rng), m(rng)};\n    }\n    return bodies;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int num_bodies = 1024;\n    int rounds = 20;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--bodies\" && i + 1 < argc) num_bodies = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    std::mt19937 rng(seed);\n    auto bodies = GenerateBodies(rng, num_bodies);\n\n    // Correctness check\n    std::vector<double> ref_fx, ref_fy, ref_fz;\n    std::vector<double> fx, fy, fz;\n    ReferenceForces(bodies, ref_fx, ref_fy, ref_fz);\n    ComputeForces(bodies, fx, fy, fz);\n    for (int i = 0; i < num_bodies; ++i) {\n        double mag = std::abs(ref_fx[i]) + std::abs(ref_fy[i]) + std::abs(ref_fz[i]);\n        double err = std::abs(fx[i] - ref_fx[i]) + std::abs(fy[i] - ref_fy[i])\n                   + std::abs(fz[i] - ref_fz[i]);\n        // Relative tolerance: allow FP reordering from SIMD/SoA/tiling\n        double tol = std::max(1e-6, mag * 1e-6);\n        if (err > tol) {\n            std::cerr << \"force mismatch at body \" << i\n                      << \" err=\" << err << \" tol=\" << tol << std::endl;\n            return 2;\n        }\n    }\n\n    // Benchmark\n    std::vector<double> latencies;\n    latencies.reserve(rounds);\n    double total_time = 0.0;\n\n    for (int r = 0; r < rounds; ++r) {\n        auto t0 = std::chrono::high_resolution_clock::now();\n        ComputeForces(bodies, fx, fy, fz);\n        auto t1 = std::chrono::high_resolution_clock::now();\n        double dt = std::chrono::duration<double>(t1 - t0).count();\n        latencies.push_back(dt);\n        total_time += dt;\n    }\n\n    double ops_per_sec = rounds / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"num_bodies\\\":\" << num_bodies << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}\n```\nKey features: Alternative approach to combined_score, Alternative approach to error\n\n### Program D2 (Score: 0.0000)\n```cpp\n#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\n// Array-of-Structures layout (deliberately naive for cache performance)\nstruct Body {\n    double x, y, z;\n    double vx, vy, vz;\n    double mass;\n};\n\n// Reference implementation (outside EVOLVE-BLOCK, used for correctness check)\nstatic void ReferenceForces(const std::vector<Body> &bodies,\n                            std::vector<double> &fx,\n                            std::vector<double> &fy,\n                            std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    for (int i = 0; i < n; ++i) {\n        for (int j = i + 1; j < n; ++j) {\n            double dx = bodies[j].x - bodies[i].x;\n            double dy = bodies[j].y - bodies[i].y;\n            double dz = bodies[j].z - bodies[i].z;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            double fi = bodies[j].mass * inv_r3;\n            double fj = bodies[i].mass * inv_r3;\n            fx[i] += dx * fi; fy[i] += dy * fi; fz[i] += dz * fi;\n            fx[j] -= dx * fj; fy[j] -= dy * fj; fz[j] -= dz * fj;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void ComputeForces(const std::vector<Body> &bodies,\n                          std::vector<double> &fx,\n                          std::vector<double> &fy,\n                          std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    \n    // Loop unrolling and optimized computation\n    for (int i = 0; i < n; ++i) {\n        const Body& bi = bodies[i];\n        const double xi = bi.x;\n        const double yi = bi.y;\n        const double zi = bi.z;\n        const double mi = bi.mass;\n        \n        for (int j = i + 1; j < n; ++j) {\n            const Body& bj = bodies[j];\n            double dx = bj.x - xi;\n            double dy = bj.y - yi;\n            double dz = bj.z - zi;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            \n            // Compute inverse square root once using fast approximation\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            \n            // Precompute force factor to avoid duplicate computation\n            double force_factor = bj.mass * inv_r3;\n            \n            // Apply forces to both bodies with reduced memory access\n            fx[i] += dx * force_factor;\n            fy[i] += dy * force_factor;\n            fz[i] += dz * force_factor;\n            \n            fx[j] -= dx * force_factor;\n            fy[j] -= dy * force_factor;\n            fz[j] -= dz * force_factor;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic std::vector<Body> GenerateBodies(std::mt19937 &rng, int n) {\n    std::uniform_real_distribution<double> pos(-100.0, 100.0);\n    std::uniform_real_distribution<double> vel(-1.0, 1.0);\n    std::uniform_real_distribution<double> m(0.1, 10.0);\n    std::vector<Body> bodies(n);\n    for (int i = 0; i < n; ++i) {\n        bodies[i] = {pos(rng), pos(rng), pos(rng),\n                     vel(rng), vel(rng), vel(rng), m(rng)};\n    }\n    return bodies;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int num_bodies = 1024;\n    int rounds = 20;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--bodies\" && i + 1 < argc) num_bodies = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    std::mt19937 rng(seed);\n    auto bodies = GenerateBodies(rng, num_bodies);\n\n    // Correctness check\n    std::vector<double> ref_fx, ref_fy, ref_fz;\n    std::vector<double> fx, fy, fz;\n    ReferenceForces(bodies, ref_fx, ref_fy, ref_fz);\n    ComputeForces(bodies, fx, fy, fz);\n    for (int i = 0; i < num_bodies; ++i) {\n        double mag = std::abs(ref_fx[i]) + std::abs(ref_fy[i]) + std::abs(ref_fz[i]);\n        double err = std::abs(fx[i] - ref_fx[i]) + std::abs(fy[i] - ref_fy[i])\n                   + std::abs(fz[i] - ref_fz[i]);\n        // Relative tolerance: allow FP reordering from SIMD/SoA/tiling\n        double tol = std::max(1e-6, mag * 1e-6);\n        if (err > tol) {\n            std::cerr << \"force mismatch at body \" << i\n                      << \" err=\" << err << \" tol=\" << tol << std::endl;\n            return 2;\n        }\n    }\n\n    // Benchmark\n    std::vector<double> latencies;\n    latencies.reserve(rounds);\n    double total_time = 0.0;\n\n    for (int r = 0; r < rounds; ++r) {\n        auto t0 = std::chrono::high_resolution_clock::now();\n        ComputeForces(bodies, fx, fy, fz);\n        auto t1 = std::chrono::high_resolution_clock::now();\n        double dt = std::chrono::duration<double>(t1 - t0).count();\n        latencies.push_back(dt);\n        total_time += dt;\n    }\n\n    double ops_per_sec = rounds / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"num_bodies\\\":\" << num_bodies << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}\n```\nKey features: Alternative approach to combined_score, Alternative approach to error\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.0000, Type: Exploratory)\n```cpp\n#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\n// Array-of-Structures layout (deliberately naive for cache performance)\nstruct Body {\n    double x, y, z;\n    double vx, vy, vz;\n    double mass;\n};\n\n// Reference implementation (outside EVOLVE-BLOCK, used for correctness check)\nstatic void ReferenceForces(const std::vector<Body> &bodies,\n                            std::vector<double> &fx,\n                            std::vector<double> &fy,\n                            std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    for (int i = 0; i < n; ++i) {\n        for (int j = i + 1; j < n; ++j) {\n            double dx = bodies[j].x - bodies[i].x;\n            double dy = bodies[j].y - bodies[i].y;\n            double dz = bodies[j].z - bodies[i].z;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            double fi = bodies[j].mass * inv_r3;\n            double fj = bodies[i].mass * inv_r3;\n            fx[i] += dx * fi; fy[i] += dy * fi; fz[i] += dz * fi;\n            fx[j] -= dx * fj; fy[j] -= dy * fj; fz[j] -= dz * fj;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void ComputeForces(const std::vector<Body> &bodies,\n                          std::vector<double> &fx,\n                          std::vector<double> &fy,\n                          std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    \n    // Optimized version with:\n    // 1. Precomputed constants\n    // 2. Reduced redundant operations\n    // 3. Better instruction scheduling\n    // 4. Avoiding repeated member access\n    // 5. Single sqrt call per pair\n    \n    for (int i = 0; i < n; ++i) {\n        const Body& bi = bodies[i];\n        const double xi = bi.x;\n        const double yi = bi.y;\n        const double zi = bi.z;\n        const double mi = bi.mass;\n        \n        for (int j = i + 1; j < n; ++j) {\n            const Body& bj = bodies[j];\n            double dx = bj.x - xi;\n            double dy = bj.y - yi;\n            double dz = bj.z - zi;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            \n            // Compute inverse square root once\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            \n            // Compute force factor once\n            double force_factor = bj.mass * inv_r3;\n            \n            // Apply forces to both bodies\n            fx[i] += dx * force_factor;\n            fy[i] += dy * force_factor;\n            fz[i] += dz * force_factor;\n            \n            fx[j] -= dx * force_factor;\n            fy[j] -= dy * force_factor;\n            fz[j] -= dz * force_factor;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic std::vector<Body> GenerateBodies(std::mt19937 &rng, int n) {\n    std::uniform_real_distribution<double> pos(-100.0, 100.0);\n    std::uniform_real_distribution<double> vel(-1.0, 1.0);\n    std::uniform_real_distribution<double> m(0.1, 10.0);\n    std::vector<Body> bodies(n);\n    for (int i = 0; i < n; ++i) {\n        bodies[i] = {pos(rng), pos(rng), pos(rng),\n                     vel(rng), vel(rng), vel(rng), m(rng)};\n    }\n    return bodies;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int num_bodies = 1024;\n    int rounds = 20;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--bodies\" && i + 1 < argc) num_bodies = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    std::mt19937 rng(seed);\n    auto bodies = GenerateBodies(rng, num_bodies);\n\n    // Correctness check\n    std::vector<double> ref_fx, ref_fy, ref_fz;\n    std::vector<double> fx, fy, fz;\n    ReferenceForces(bodies, ref_fx, ref_fy, ref_fz);\n    ComputeForces(bodies, fx, fy, fz);\n    for (int i = 0; i < num_bodies; ++i) {\n        double mag = std::abs(ref_fx[i]) + std::abs(ref_fy[i]) + std::abs(ref_fz[i]);\n        double err = std::abs(fx[i] - ref_fx[i]) + std::abs(fy[i] - ref_fy[i])\n                   + std::abs(fz[i] - ref_fz[i]);\n        // Relative tolerance: allow FP reordering from SIMD/SoA/tiling\n        double tol = std::max(1e-6, mag * 1e-6);\n        if (err > tol) {\n            std::cerr << \"force mismatch at body \" << i\n                      << \" err=\" << err << \" tol=\" << tol << std::endl;\n            return 2;\n        }\n    }\n\n    // Benchmark\n    std::vector<double> latencies;\n    latencies.reserve(rounds);\n    double total_time = 0.0;\n\n    for (int r = 0; r < rounds; ++r) {\n        auto t0 = std::chrono::high_resolution_clock::now();\n        ComputeForces(bodies, fx, fy, fz);\n        auto t1 = std::chrono::high_resolution_clock::now();\n        double dt = std::chrono::duration<double>(t1 - t0).count();\n        latencies.push_back(dt);\n        total_time += dt;\n    }\n\n    double ops_per_sec = rounds / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"num_bodies\\\":\" << num_bodies << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.0000, Type: Exploratory)\n```cpp\n#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\n// Array-of-Structures layout (deliberately naive for cache performance)\nstruct Body {\n    double x, y, z;\n    double vx, vy, vz;\n    double mass;\n};\n\n// Reference implementation (outside EVOLVE-BLOCK, used for correctness check)\nstatic void ReferenceForces(const std::vector<Body> &bodies,\n                            std::vector<double> &fx,\n                            std::vector<double> &fy,\n                            std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    for (int i = 0; i < n; ++i) {\n        for (int j = i + 1; j < n; ++j) {\n            double dx = bodies[j].x - bodies[i].x;\n            double dy = bodies[j].y - bodies[i].y;\n            double dz = bodies[j].z - bodies[i].z;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            double fi = bodies[j].mass * inv_r3;\n            double fj = bodies[i].mass * inv_r3;\n            fx[i] += dx * fi; fy[i] += dy * fi; fz[i] += dz * fi;\n            fx[j] -= dx * fj; fy[j] -= dy * fj; fz[j] -= dz * fj;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void ComputeForces(const std::vector<Body> &bodies,\n                          std::vector<double> &fx,\n                          std::vector<double> &fy,\n                          std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    \n    // Loop unrolling and optimized computation\n    for (int i = 0; i < n; ++i) {\n        const Body& bi = bodies[i];\n        const double xi = bi.x;\n        const double yi = bi.y;\n        const double zi = bi.z;\n        const double mi = bi.mass;\n        \n        for (int j = i + 1; j < n; ++j) {\n            const Body& bj = bodies[j];\n            double dx = bj.x - xi;\n            double dy = bj.y - yi;\n            double dz = bj.z - zi;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            \n            // Compute inverse square root once using fast approximation\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            \n            // Precompute force factor to avoid duplicate computation\n            double force_factor = bj.mass * inv_r3;\n            \n            // Apply forces to both bodies with reduced memory access\n            fx[i] += dx * force_factor;\n            fy[i] += dy * force_factor;\n            fz[i] += dz * force_factor;\n            \n            fx[j] -= dx * force_factor;\n            fy[j] -= dy * force_factor;\n            fz[j] -= dz * force_factor;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic std::vector<Body> GenerateBodies(std::mt19937 &rng, int n) {\n    std::uniform_real_distribution<double> pos(-100.0, 100.0);\n    std::uniform_real_distribution<double> vel(-1.0, 1.0);\n    std::uniform_real_distribution<double> m(0.1, 10.0);\n    std::vector<Body> bodies(n);\n    for (int i = 0; i < n; ++i) {\n        bodies[i] = {pos(rng), pos(rng), pos(rng),\n                     vel(rng), vel(rng), vel(rng), m(rng)};\n    }\n    return bodies;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int num_bodies = 1024;\n    int rounds = 20;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--bodies\" && i + 1 < argc) num_bodies = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    std::mt19937 rng(seed);\n    auto bodies = GenerateBodies(rng, num_bodies);\n\n    // Correctness check\n    std::vector<double> ref_fx, ref_fy, ref_fz;\n    std::vector<double> fx, fy, fz;\n    ReferenceForces(bodies, ref_fx, ref_fy, ref_fz);\n    ComputeForces(bodies, fx, fy, fz);\n    for (int i = 0; i < num_bodies; ++i) {\n        double mag = std::abs(ref_fx[i]) + std::abs(ref_fy[i]) + std::abs(ref_fz[i]);\n        double err = std::abs(fx[i] - ref_fx[i]) + std::abs(fy[i] - ref_fy[i])\n                   + std::abs(fz[i] - ref_fz[i]);\n        // Relative tolerance: allow FP reordering from SIMD/SoA/tiling\n        double tol = std::max(1e-6, mag * 1e-6);\n        if (err > tol) {\n            std::cerr << \"force mismatch at body \" << i\n                      << \" err=\" << err << \" tol=\" << tol << std::endl;\n            return 2;\n        }\n    }\n\n    // Benchmark\n    std::vector<double> latencies;\n    latencies.reserve(rounds);\n    double total_time = 0.0;\n\n    for (int r = 0; r < rounds; ++r) {\n        auto t0 = std::chrono::high_resolution_clock::now();\n        ComputeForces(bodies, fx, fy, fz);\n        auto t1 = std::chrono::high_resolution_clock::now();\n        double dt = std::chrono::duration<double>(t1 - t0).count();\n        latencies.push_back(dt);\n        total_time += dt;\n    }\n\n    double ops_per_sec = rounds / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"num_bodies\\\":\" << num_bodies << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 0.0000, Type: Exploratory)\n```cpp\n#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\n// Array-of-Structures layout (deliberately naive for cache performance)\nstruct Body {\n    double x, y, z;\n    double vx, vy, vz;\n    double mass;\n};\n\n// Reference implementation (outside EVOLVE-BLOCK, used for correctness check)\nstatic void ReferenceForces(const std::vector<Body> &bodies,\n                            std::vector<double> &fx,\n                            std::vector<double> &fy,\n                            std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    for (int i = 0; i < n; ++i) {\n        for (int j = i + 1; j < n; ++j) {\n            double dx = bodies[j].x - bodies[i].x;\n            double dy = bodies[j].y - bodies[i].y;\n            double dz = bodies[j].z - bodies[i].z;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            double fi = bodies[j].mass * inv_r3;\n            double fj = bodies[i].mass * inv_r3;\n            fx[i] += dx * fi; fy[i] += dy * fi; fz[i] += dz * fi;\n            fx[j] -= dx * fj; fy[j] -= dy * fj; fz[j] -= dz * fj;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void ComputeForces(const std::vector<Body> &bodies,\n                          std::vector<double> &fx,\n                          std::vector<double> &fy,\n                          std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    \n    // Optimized version with:\n    // 1. Precomputed constants\n    // 2. Reduced redundant operations\n    // 3. Better instruction scheduling\n    // 4. Avoiding repeated member access\n    \n    for (int i = 0; i < n; ++i) {\n        const Body& bi = bodies[i];\n        const double xi = bi.x;\n        const double yi = bi.y;\n        const double zi = bi.z;\n        const double mi = bi.mass;\n        \n        for (int j = i + 1; j < n; ++j) {\n            const Body& bj = bodies[j];\n            double dx = bj.x - xi;\n            double dy = bj.y - yi;\n            double dz = bj.z - zi;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            \n            // Compute inverse square root once\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            \n            // Compute force factor once\n            double force_factor = bj.mass * inv_r3;\n            \n            // Apply forces to both bodies\n            fx[i] += dx * force_factor;\n            fy[i] += dy * force_factor;\n            fz[i] += dz * force_factor;\n            \n            fx[j] -= dx * force_factor;\n            fy[j] -= dy * force_factor;\n            fz[j] -= dz * force_factor;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic std::vector<Body> GenerateBodies(std::mt19937 &rng, int n) {\n    std::uniform_real_distribution<double> pos(-100.0, 100.0);\n    std::uniform_real_distribution<double> vel(-1.0, 1.0);\n    std::uniform_real_distribution<double> m(0.1, 10.0);\n    std::vector<Body> bodies(n);\n    for (int i = 0; i < n; ++i) {\n        bodies[i] = {pos(rng), pos(rng), pos(rng),\n                     vel(rng), vel(rng), vel(rng), m(rng)};\n    }\n    return bodies;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int num_bodies = 1024;\n    int rounds = 20;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--bodies\" && i + 1 < argc) num_bodies = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    std::mt19937 rng(seed);\n    auto bodies = GenerateBodies(rng, num_bodies);\n\n    // Correctness check\n    std::vector<double> ref_fx, ref_fy, ref_fz;\n    std::vector<double> fx, fy, fz;\n    ReferenceForces(bodies, ref_fx, ref_fy, ref_fz);\n    ComputeForces(bodies, fx, fy, fz);\n    for (int i = 0; i < num_bodies; ++i) {\n        double mag = std::abs(ref_fx[i]) + std::abs(ref_fy[i]) + std::abs(ref_fz[i]);\n        double err = std::abs(fx[i] - ref_fx[i]) + std::abs(fy[i] - ref_fy[i])\n                   + std::abs(fz[i] - ref_fz[i]);\n        // Relative tolerance: allow FP reordering from SIMD/SoA/tiling\n        double tol = std::max(1e-6, mag * 1e-6);\n        if (err > tol) {\n            std::cerr << \"force mismatch at body \" << i\n                      << \" err=\" << err << \" tol=\" << tol << std::endl;\n            return 2;\n        }\n    }\n\n    // Benchmark\n    std::vector<double> latencies;\n    latencies.reserve(rounds);\n    double total_time = 0.0;\n\n    for (int r = 0; r < rounds; ++r) {\n        auto t0 = std::chrono::high_resolution_clock::now();\n        ComputeForces(bodies, fx, fy, fz);\n        auto t1 = std::chrono::high_resolution_clock::now();\n        double dt = std::chrono::duration<double>(t1 - t0).count();\n        latencies.push_back(dt);\n        total_time += dt;\n    }\n\n    double ops_per_sec = rounds / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"num_bodies\\\":\" << num_bodies << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```cpp\n#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\n// Array-of-Structures layout (deliberately naive for cache performance)\nstruct Body {\n    double x, y, z;\n    double vx, vy, vz;\n    double mass;\n};\n\n// Reference implementation (outside EVOLVE-BLOCK, used for correctness check)\nstatic void ReferenceForces(const std::vector<Body> &bodies,\n                            std::vector<double> &fx,\n                            std::vector<double> &fy,\n                            std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    for (int i = 0; i < n; ++i) {\n        for (int j = i + 1; j < n; ++j) {\n            double dx = bodies[j].x - bodies[i].x;\n            double dy = bodies[j].y - bodies[i].y;\n            double dz = bodies[j].z - bodies[i].z;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            double fi = bodies[j].mass * inv_r3;\n            double fj = bodies[i].mass * inv_r3;\n            fx[i] += dx * fi; fy[i] += dy * fi; fz[i] += dz * fi;\n            fx[j] -= dx * fj; fy[j] -= dy * fj; fz[j] -= dz * fj;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void ComputeForces(const std::vector<Body> &bodies,\n                          std::vector<double> &fx,\n                          std::vector<double> &fy,\n                          std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    \n    // Optimized version with:\n    // 1. Precomputed constants\n    // 2. Reduced redundant operations\n    // 3. Better instruction scheduling\n    // 4. Avoiding repeated member access\n    // 5. Loop unrolling for better performance\n    \n    for (int i = 0; i < n; ++i) {\n        const Body& bi = bodies[i];\n        const double xi = bi.x;\n        const double yi = bi.y;\n        const double zi = bi.z;\n        const double mi = bi.mass;\n        \n        for (int j = i + 1; j < n; ++j) {\n            const Body& bj = bodies[j];\n            double dx = bj.x - xi;\n            double dy = bj.y - yi;\n            double dz = bj.z - zi;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            \n            // Compute inverse square root once using fast approximation\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            \n            // Compute force factor once to avoid duplicate computation\n            double force_factor = bj.mass * inv_r3;\n            \n            // Apply forces to both bodies with reduced memory access\n            fx[i] += dx * force_factor;\n            fy[i] += dy * force_factor;\n            fz[i] += dz * force_factor;\n            \n            fx[j] -= dx * force_factor;\n            fy[j] -= dy * force_factor;\n            fz[j] -= dz * force_factor;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic std::vector<Body> GenerateBodies(std::mt19937 &rng, int n) {\n    std::uniform_real_distribution<double> pos(-100.0, 100.0);\n    std::uniform_real_distribution<double> vel(-1.0, 1.0);\n    std::uniform_real_distribution<double> m(0.1, 10.0);\n    std::vector<Body> bodies(n);\n    for (int i = 0; i < n; ++i) {\n        bodies[i] = {pos(rng), pos(rng), pos(rng),\n                     vel(rng), vel(rng), vel(rng), m(rng)};\n    }\n    return bodies;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int num_bodies = 1024;\n    int rounds = 20;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--bodies\" && i + 1 < argc) num_bodies = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    std::mt19937 rng(seed);\n    auto bodies = GenerateBodies(rng, num_bodies);\n\n    // Correctness check\n    std::vector<double> ref_fx, ref_fy, ref_fz;\n    std::vector<double> fx, fy, fz;\n    ReferenceForces(bodies, ref_fx, ref_fy, ref_fz);\n    ComputeForces(bodies, fx, fy, fz);\n    for (int i = 0; i < num_bodies; ++i) {\n        double mag = std::abs(ref_fx[i]) + std::abs(ref_fy[i]) + std::abs(ref_fz[i]);\n        double err = std::abs(fx[i] - ref_fx[i]) + std::abs(fy[i] - ref_fy[i])\n                   + std::abs(fz[i] - ref_fz[i]);\n        // Relative tolerance: allow FP reordering from SIMD/SoA/tiling\n        double tol = std::max(1e-6, mag * 1e-6);\n        if (err > tol) {\n            std::cerr << \"force mismatch at body \" << i\n                      << \" err=\" << err << \" tol=\" << tol << std::endl;\n            return 2;\n        }\n    }\n\n    // Benchmark\n    std::vector<double> latencies;\n    latencies.reserve(rounds);\n    double total_time = 0.0;\n\n    for (int r = 0; r < rounds; ++r) {\n        auto t0 = std::chrono::high_resolution_clock::now();\n        ComputeForces(bodies, fx, fy, fz);\n        auto t1 = std::chrono::high_resolution_clock::now();\n        double dt = std::chrono::duration<double>(t1 - t0).count();\n        latencies.push_back(dt);\n        total_time += dt;\n    }\n\n    double ops_per_sec = rounds / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"num_bodies\\\":\" << num_bodies << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: ops_per_sec, p99_latency_us\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```cpp\n# Your rewritten program here\n```", "responses": ["```cpp\n#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\n// Array-of-Structures layout (deliberately naive for cache performance)\nstruct Body {\n    double x, y, z;\n    double vx, vy, vz;\n    double mass;\n};\n\n// Reference implementation (outside EVOLVE-BLOCK, used for correctness check)\nstatic void ReferenceForces(const std::vector<Body> &bodies,\n                            std::vector<double> &fx,\n                            std::vector<double> &fy,\n                            std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    for (int i = 0; i < n; ++i) {\n        for (int j = i + 1; j < n; ++j) {\n            double dx = bodies[j].x - bodies[i].x;\n            double dy = bodies[j].y - bodies[i].y;\n            double dz = bodies[j].z - bodies[i].z;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            double fi = bodies[j].mass * inv_r3;\n            double fj = bodies[i].mass * inv_r3;\n            fx[i] += dx * fi; fy[i] += dy * fi; fz[i] += dz * fi;\n            fx[j] -= dx * fj; fy[j] -= dy * fj; fz[j] -= dz * fj;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void ComputeForces(const std::vector<Body> &bodies,\n                          std::vector<double> &fx,\n                          std::vector<double> &fy,\n                          std::vector<double> &fz) {\n    int n = (int)bodies.size();\n    fx.assign(n, 0.0);\n    fy.assign(n, 0.0);\n    fz.assign(n, 0.0);\n    const double eps2 = 1e-9;\n    \n    // Highly optimized version with:\n    // 1. Loop unrolling to reduce loop overhead\n    // 2. Pre-computed values to minimize redundant calculations\n    // 3. Better instruction scheduling for modern CPUs\n    // 4. Reduced memory access patterns\n    // 5. Single sqrt call per pair for accuracy\n    \n    for (int i = 0; i < n; ++i) {\n        const Body& bi = bodies[i];\n        const double xi = bi.x;\n        const double yi = bi.y;\n        const double zi = bi.z;\n        const double mi = bi.mass;\n        \n        // Process 4 bodies at a time to reduce loop overhead\n        int j = i + 1;\n        for (; j <= n - 4; j += 4) {\n            // Process four pairs simultaneously\n            const Body& bj0 = bodies[j];\n            const Body& bj1 = bodies[j+1];\n            const Body& bj2 = bodies[j+2];\n            const Body& bj3 = bodies[j+3];\n            \n            // First pair (i, j)\n            double dx0 = bj0.x - xi;\n            double dy0 = bj0.y - yi;\n            double dz0 = bj0.z - zi;\n            double r2_0 = dx0 * dx0 + dy0 * dy0 + dz0 * dz0 + eps2;\n            double inv_r0 = 1.0 / std::sqrt(r2_0);\n            double inv_r3_0 = inv_r0 * inv_r0 * inv_r0;\n            double force_factor_0 = bj0.mass * inv_r3_0;\n            \n            fx[i] += dx0 * force_factor_0;\n            fy[i] += dy0 * force_factor_0;\n            fz[i] += dz0 * force_factor_0;\n            \n            fx[j] -= dx0 * force_factor_0;\n            fy[j] -= dy0 * force_factor_0;\n            fz[j] -= dz0 * force_factor_0;\n            \n            // Second pair (i, j+1)\n            double dx1 = bj1.x - xi;\n            double dy1 = bj1.y - yi;\n            double dz1 = bj1.z - zi;\n            double r2_1 = dx1 * dx1 + dy1 * dy1 + dz1 * dz1 + eps2;\n            double inv_r1 = 1.0 / std::sqrt(r2_1);\n            double inv_r3_1 = inv_r1 * inv_r1 * inv_r1;\n            double force_factor_1 = bj1.mass * inv_r3_1;\n            \n            fx[i] += dx1 * force_factor_1;\n            fy[i] += dy1 * force_factor_1;\n            fz[i] += dz1 * force_factor_1;\n            \n            fx[j+1] -= dx1 * force_factor_1;\n            fy[j+1] -= dy1 * force_factor_1;\n            fz[j+1] -= dz1 * force_factor_1;\n            \n            // Third pair (i, j+2)\n            double dx2 = bj2.x - xi;\n            double dy2 = bj2.y - yi;\n            double dz2 = bj2.z - zi;\n            double r2_2 = dx2 * dx2 + dy2 * dy2 + dz2 * dz2 + eps2;\n            double inv_r2 = 1.0 / std::sqrt(r2_2);\n            double inv_r3_2 = inv_r2 * inv_r2 * inv_r3_2;\n            double force_factor_2 = bj2.mass * inv_r3_2;\n            \n            fx[i] += dx2 * force_factor_2;\n            fy[i] += dy2 * force_factor_2;\n            fz[i] += dz2 * force_factor_2;\n            \n            fx[j+2] -= dx2 * force_factor_2;\n            fy[j+2] -= dy2 * force_factor_2;\n            fz[j+2] -= dz2 * force_factor_2;\n            \n            // Fourth pair (i, j+3)\n            double dx3 = bj3.x - xi;\n            double dy3 = bj3.y - yi;\n            double dz3 = bj3.z - zi;\n            double r2_3 = dx3 * dx3 + dy3 * dy3 + dz3 * dz3 + eps2;\n            double inv_r3 = 1.0 / std::sqrt(r2_3);\n            double inv_r3_3 = inv_r3 * inv_r3 * inv_r3;\n            double force_factor_3 = bj3.mass * inv_r3_3;\n            \n            fx[i] += dx3 * force_factor_3;\n            fy[i] += dy3 * force_factor_3;\n            fz[i] += dz3 * force_factor_3;\n            \n            fx[j+3] -= dx3 * force_factor_3;\n            fy[j+3] -= dy3 * force_factor_3;\n            fz[j+3] -= dz3 * force_factor_3;\n        }\n        \n        // Handle remaining pairs\n        for (; j < n; ++j) {\n            const Body& bj = bodies[j];\n            double dx = bj.x - xi;\n            double dy = bj.y - yi;\n            double dz = bj.z - zi;\n            double r2 = dx * dx + dy * dy + dz * dz + eps2;\n            double inv_r = 1.0 / std::sqrt(r2);\n            double inv_r3 = inv_r * inv_r * inv_r;\n            double force_factor = bj.mass * inv_r3;\n            \n            fx[i] += dx * force_factor;\n            fy[i] += dy * force_factor;\n            fz[i] += dz * force_factor;\n            \n            fx[j] -= dx * force_factor;\n            fy[j] -= dy * force_factor;\n            fz[j] -= dz * force_factor;\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic std::vector<Body> GenerateBodies(std::mt19937 &rng, int n) {\n    std::uniform_real_distribution<double> pos(-100.0, 100.0);\n    std::uniform_real_distribution<double> vel(-1.0, 1.0);\n    std::uniform_real_distribution<double> m(0.1, 10.0);\n    std::vector<Body> bodies(n);\n    for (int i = 0; i < n; ++i) {\n        bodies[i] = {pos(rng), pos(rng), pos(rng),\n                     vel(rng), vel(rng), vel(rng), m(rng)};\n    }\n    return bodies;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int num_bodies = 1024;\n    int rounds = 20;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--bodies\" && i + 1 < argc) num_bodies = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    std::mt19937 rng(seed);\n    auto bodies = GenerateBodies(rng, num_bodies);\n\n    // Correctness check\n    std::vector<double> ref_fx, ref_fy, ref_fz;\n    std::vector<double> fx, fy, fz;\n    ReferenceForces(bodies, ref_fx, ref_fy, ref_fz);\n    ComputeForces(bodies, fx, fy, fz);\n    for (int i = 0; i < num_bodies; ++i) {\n        double mag = std::abs(ref_fx[i]) + std::abs(ref_fy[i]) + std::abs(ref_fz[i]);\n        double err = std::abs(fx[i] - ref_fx[i]) + std::abs(fy[i] - ref_fy[i])\n                   + std::abs(fz[i] - ref_fz[i]);\n        // Relative tolerance: allow FP reordering from SIMD/SoA/tiling\n        double tol = std::max(1e-6, mag * 1e-6);\n        if (err > tol) {\n            std::cerr << \"force mismatch at body \" << i\n                      << \" err=\" << err << \" tol=\" << tol << std::endl;\n            return 2;\n        }\n    }\n\n    // Benchmark\n    std::vector<double> latencies;\n    latencies.reserve(rounds);\n    double total_time = 0.0;\n\n    for (int r = 0; r < rounds; ++r) {\n        auto t0 = std::chrono::high_resolution_clock::now();\n        ComputeForces(bodies, fx, fy, fz);\n        auto t1 = std::chrono::high_resolution_clock::now();\n        double dt = std::chrono::duration<double>(t1 - t0).count();\n        latencies.push_back(dt);\n        total_time += dt;\n    }\n\n    double ops_per_sec = rounds / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"num_bodies\\\":\" << num_bodies << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}\n```"]}}, "artifacts_json": null, "artifact_dir": null, "embedding": null}