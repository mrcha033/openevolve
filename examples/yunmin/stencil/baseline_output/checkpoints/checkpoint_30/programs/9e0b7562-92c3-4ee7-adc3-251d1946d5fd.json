{"id": "9e0b7562-92c3-4ee7-adc3-251d1946d5fd", "code": "#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\nstatic const int GRID_N = 1024;\n\n// Reference stencil step (outside EVOLVE-BLOCK)\nstatic void ReferenceStep(const std::vector<double> &in,\n                          std::vector<double> &out, int N) {\n    for (int i = 1; i < N - 1; ++i) {\n        for (int j = 1; j < N - 1; ++j) {\n            out[i * N + j] = 0.25 * (in[(i - 1) * N + j] + in[(i + 1) * N + j]\n                                   + in[i * N + (j - 1)] + in[i * N + (j + 1)]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void StencilStep(const std::vector<double> &in,\n                        std::vector<double> &out, int N) {\n    // 5-point Jacobi stencil: average of 4 neighbors\n    // Highly optimized version with minimal overhead and improved cache locality\n    const int Nm2 = N - 2;\n    \n    // Process rows sequentially with optimized loop structure\n    for (int i = 1; i <= Nm2; ++i) {\n        const int base_i = i * N;\n        const int base_im1 = base_i - N;  // (i-1) * N\n        const int base_ip1 = base_i + N;  // (i+1) * N\n        \n        // Unroll inner loop by 4 for better instruction-level parallelism\n        int j = 1;\n        const int j_end = Nm2 - 3;\n        for (; j <= j_end; j += 4) {\n            const int base_j = base_i + j;\n            \n            // Compute four stencil points simultaneously using precomputed offsets\n            const double val0 = in[base_im1 + j] + in[base_ip1 + j] +\n                               in[base_i + j - 1] + in[base_i + j + 1];\n            const double val1 = in[base_im1 + j + 1] + in[base_ip1 + j + 1] +\n                               in[base_i + j] + in[base_i + j + 2];\n            const double val2 = in[base_im1 + j + 2] + in[base_ip1 + j + 2] +\n                               in[base_i + j + 1] + in[base_i + j + 3];\n            const double val3 = in[base_im1 + j + 3] + in[base_ip1 + j + 3] +\n                               in[base_i + j + 2] + in[base_i + j + 4];\n            \n            out[base_j] = 0.25 * val0;\n            out[base_j + 1] = 0.25 * val1;\n            out[base_j + 2] = 0.25 * val2;\n            out[base_j + 3] = 0.25 * val3;\n        }\n        \n        // Handle remaining elements\n        for (; j <= Nm2; ++j) {\n            const int base_j = base_i + j;\n            out[base_j] = 0.25 * (in[base_im1 + j] + in[base_ip1 + j] +\n                                 in[base_i + j - 1] + in[base_i + j + 1]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic double GridChecksum(const std::vector<double> &grid, int N) {\n    double sum = 0.0;\n    for (int i = 0; i < N * N; ++i) sum += grid[i];\n    return sum;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int N = GRID_N;\n    int timesteps = 100;\n    int rounds = 5;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--grid\" && i + 1 < argc) N = std::atoi(argv[++i]);\n        else if (arg == \"--timesteps\" && i + 1 < argc) timesteps = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    // Initialize grid with deterministic values; boundaries stay 0\n    std::mt19937 rng(seed);\n    std::uniform_real_distribution<double> dist(0.0, 1.0);\n    std::vector<double> grid_a(N * N, 0.0);\n    std::vector<double> grid_b(N * N, 0.0);\n    for (int i = 1; i < N - 1; ++i)\n        for (int j = 1; j < N - 1; ++j)\n            grid_a[i * N + j] = dist(rng);\n\n    // Reference grids for correctness check (run 10 steps)\n    std::vector<double> ref_a(grid_a), ref_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        ReferenceStep(ref_a, ref_b, N);\n        std::swap(ref_a, ref_b);\n    }\n    double ref_checksum = GridChecksum(ref_a, N);\n\n    // Evolved function: same 10 steps\n    std::vector<double> test_a(grid_a), test_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        StencilStep(test_a, test_b, N);\n        std::swap(test_a, test_b);\n    }\n    double test_checksum = GridChecksum(test_a, N);\n\n    if (std::abs(ref_checksum - test_checksum) > 1e-6) {\n        std::cerr << \"stencil mismatch: ref=\" << ref_checksum\n                  << \" got=\" << test_checksum << std::endl;\n        return 2;\n    }\n\n    // Benchmark: each round runs `timesteps` stencil steps\n    std::vector<double> latencies;\n    latencies.reserve(rounds * timesteps);\n    double total_time = 0.0;\n    long long total_steps = 0;\n\n    for (int r = 0; r < rounds; ++r) {\n        // Re-init grid each round for consistent measurement\n        std::copy(grid_a.begin(), grid_a.end(), test_a.begin());\n        std::fill(test_b.begin(), test_b.end(), 0.0);\n\n        for (int t = 0; t < timesteps; ++t) {\n            auto t0 = std::chrono::high_resolution_clock::now();\n            StencilStep(test_a, test_b, N);\n            auto t1 = std::chrono::high_resolution_clock::now();\n            double dt = std::chrono::duration<double>(t1 - t0).count();\n            latencies.push_back(dt);\n            total_time += dt;\n            ++total_steps;\n            std::swap(test_a, test_b);\n        }\n    }\n\n    double ops_per_sec = total_steps / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n    // Each step updates (N-2)*(N-2) cells, 5 FLOP each\n    double gflops = (double)(N - 2) * (N - 2) * 5.0 * total_steps\n                  / total_time / 1e9;\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"gflops\\\":\" << gflops\n       << \",\\\"grid_size\\\":\" << N << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}", "changes_description": null, "language": "cpp", "parent_id": "aba6ef01-c364-405c-a11c-cdbaa211d4bf", "generation": 3, "timestamp": 1770563250.4071288, "iteration_found": 15, "metrics": {"combined_score": 1.0649, "ops_per_sec": 1698.098071, "p99_latency_us": 595.621, "bcoz_max_speedup": 5.0, "bperf_offcpu_ratio": 0.02, "gflops": 8.868181}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"combined_score": 1.0339, "ops_per_sec": 1654.799115, "p99_latency_us": 618.895, "bcoz_max_speedup": 5.0, "bperf_offcpu_ratio": 0.02, "gflops": 8.642056}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are a C++ performance optimization expert.\nOptimize the 2D stencil computation inside the EVOLVE-BLOCK.\n\nRequirements:\n- Only modify code inside the EVOLVE-BLOCK.\n- Keep the function signature: void StencilStep(const vector<double>&, vector<double>&, int N)\n- Must compute 5-point Jacobi stencil: out[i][j] = 0.25 * (in[i-1][j] + in[i+1][j] + in[i][j-1] + in[i][j+1])\n- Only update interior cells (i,j in [1..N-2]).\n- Results must match the reference within 1e-6 absolute error (checksum).\n- Use only the C++ standard library. No external dependencies.\n- Do not add top-level side effects or debug printing.\n\nMake the stencil computation as fast as possible.\n", "user": "# Current Program Information\n- Fitness: 1.0339\n- Feature coordinates: ops_per_sec=1654.80, p99_latency_us=618.89\n- Focus areas: - Fitness declined: 1.0359 \u2192 1.0339. Consider revising recent changes.\n- Exploring ops_per_sec=1654.80, p99_latency_us=618.89 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: combined_score: 1.0359, ops_per_sec: 1658.4638, p99_latency_us: 618.0710, bcoz_max_speedup: 5.0000, bperf_offcpu_ratio: 0.0200, gflops: 8.6612\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: combined_score: 1.0394, ops_per_sec: 1657.7433, p99_latency_us: 610.4780, bcoz_max_speedup: 5.0000, bperf_offcpu_ratio: 0.0200, gflops: 8.6574\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: combined_score: 1.0572, ops_per_sec: 1685.4846, p99_latency_us: 599.6240, bcoz_max_speedup: 5.0000, bperf_offcpu_ratio: 0.0200, gflops: 8.8023\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 1.0572)\n```cpp\n#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\nstatic const int GRID_N = 1024;\n\n// Reference stencil step (outside EVOLVE-BLOCK)\nstatic void ReferenceStep(const std::vector<double> &in,\n                          std::vector<double> &out, int N) {\n    for (int i = 1; i < N - 1; ++i) {\n        for (int j = 1; j < N - 1; ++j) {\n            out[i * N + j] = 0.25 * (in[(i - 1) * N + j] + in[(i + 1) * N + j]\n                                   + in[i * N + (j - 1)] + in[i * N + (j + 1)]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void StencilStep(const std::vector<double> &in,\n                        std::vector<double> &out, int N) {\n    // 5-point Jacobi stencil: average of 4 neighbors\n    // Optimized version with loop unrolling and reduced memory access\n    const int Nm2 = N - 2;\n    \n    // Process rows in chunks to improve cache locality\n    for (int i = 1; i <= Nm2; ++i) {\n        const int base_i = i * N;\n        const int base_im1 = (i - 1) * N;\n        const int base_ip1 = (i + 1) * N;\n        \n        // Unroll inner loop by 4 for better instruction-level parallelism\n        int j = 1;\n        for (; j <= Nm2 - 3; j += 4) {\n            const int base_j = base_i + j;\n            \n            // Compute four stencil points simultaneously\n            out[base_j] = 0.25 * (in[base_im1 + j] + in[base_ip1 + j] +\n                                 in[base_i + j - 1] + in[base_i + j + 1]);\n            out[base_j + 1] = 0.25 * (in[base_im1 + j + 1] + in[base_ip1 + j + 1] +\n                                     in[base_i + j] + in[base_i + j + 2]);\n            out[base_j + 2] = 0.25 * (in[base_im1 + j + 2] + in[base_ip1 + j + 2] +\n                                     in[base_i + j + 1] + in[base_i + j + 3]);\n            out[base_j + 3] = 0.25 * (in[base_im1 + j + 3] + in[base_ip1 + j + 3] +\n                                     in[base_i + j + 2] + in[base_i + j + 4]);\n        }\n        \n        // Handle remaining elements\n        for (; j <= Nm2; ++j) {\n            const int base_j = base_i + j;\n            out[base_j] = 0.25 * (in[base_im1 + j] + in[base_ip1 + j] +\n                                 in[base_i + j - 1] + in[base_i + j + 1]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic double GridChecksum(const std::vector<double> &grid, int N) {\n    double sum = 0.0;\n    for (int i = 0; i < N * N; ++i) sum += grid[i];\n    return sum;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int N = GRID_N;\n    int timesteps = 100;\n    int rounds = 5;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--grid\" && i + 1 < argc) N = std::atoi(argv[++i]);\n        else if (arg == \"--timesteps\" && i + 1 < argc) timesteps = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    // Initialize grid with deterministic values; boundaries stay 0\n    std::mt19937 rng(seed);\n    std::uniform_real_distribution<double> dist(0.0, 1.0);\n    std::vector<double> grid_a(N * N, 0.0);\n    std::vector<double> grid_b(N * N, 0.0);\n    for (int i = 1; i < N - 1; ++i)\n        for (int j = 1; j < N - 1; ++j)\n            grid_a[i * N + j] = dist(rng);\n\n    // Reference grids for correctness check (run 10 steps)\n    std::vector<double> ref_a(grid_a), ref_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        ReferenceStep(ref_a, ref_b, N);\n        std::swap(ref_a, ref_b);\n    }\n    double ref_checksum = GridChecksum(ref_a, N);\n\n    // Evolved function: same 10 steps\n    std::vector<double> test_a(grid_a), test_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        StencilStep(test_a, test_b, N);\n        std::swap(test_a, test_b);\n    }\n    double test_checksum = GridChecksum(test_a, N);\n\n    if (std::abs(ref_checksum - test_checksum) > 1e-6) {\n        std::cerr << \"stencil mismatch: ref=\" << ref_checksum\n                  << \" got=\" << test_checksum << std::endl;\n        return 2;\n    }\n\n    // Benchmark: each round runs `timesteps` stencil steps\n    std::vector<double> latencies;\n    latencies.reserve(rounds * timesteps);\n    double total_time = 0.0;\n    long long total_steps = 0;\n\n    for (int r = 0; r < rounds; ++r) {\n        // Re-init grid each round for consistent measurement\n        std::copy(grid_a.begin(), grid_a.end(), test_a.begin());\n        std::fill(test_b.begin(), test_b.end(), 0.0);\n\n        for (int t = 0; t < timesteps; ++t) {\n            auto t0 = std::chrono::high_resolution_clock::now();\n            StencilStep(test_a, test_b, N);\n            auto t1 = std::chrono::high_resolution_clock::now();\n            double dt = std::chrono::duration<double>(t1 - t0).count();\n            latencies.push_back(dt);\n            total_time += dt;\n            ++total_steps;\n            std::swap(test_a, test_b);\n        }\n    }\n\n    double ops_per_sec = total_steps / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n    // Each step updates (N-2)*(N-2) cells, 5 FLOP each\n    double gflops = (double)(N - 2) * (N - 2) * 5.0 * total_steps\n                  / total_time / 1e9;\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"gflops\\\":\" << gflops\n       << \",\\\"grid_size\\\":\" << N << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}\n```\nKey features: Performs well on combined_score (1.0572), Performs well on ops_per_sec (1685.4846), Performs well on p99_latency_us (599.6240), Performs well on bcoz_max_speedup (5.0000), Performs well on bperf_offcpu_ratio (0.0200), Performs well on gflops (8.8023)\n\n### Program 2 (Score: 1.0394)\n```cpp\n#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\nstatic const int GRID_N = 1024;\n\n// Reference stencil step (outside EVOLVE-BLOCK)\nstatic void ReferenceStep(const std::vector<double> &in,\n                          std::vector<double> &out, int N) {\n    for (int i = 1; i < N - 1; ++i) {\n        for (int j = 1; j < N - 1; ++j) {\n            out[i * N + j] = 0.25 * (in[(i - 1) * N + j] + in[(i + 1) * N + j]\n                                   + in[i * N + (j - 1)] + in[i * N + (j + 1)]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void StencilStep(const std::vector<double> &in,\n                        std::vector<double> &out, int N) {\n    // 5-point Jacobi stencil: average of 4 neighbors\n    // Highly optimized version with loop unrolling and cache-friendly access\n    const int Nm2 = N - 2;\n    \n    // Process rows in chunks to improve cache locality\n    for (int i = 1; i <= Nm2; ++i) {\n        const int base_i = i * N;\n        const int base_im1 = (i - 1) * N;\n        const int base_ip1 = (i + 1) * N;\n        \n        // Unroll inner loop by 4 for better instruction-level parallelism\n        int j = 1;\n        for (; j <= Nm2 - 3; j += 4) {\n            const int base_j = base_i + j;\n            \n            // Compute four stencil points simultaneously\n            out[base_j] = 0.25 * (in[base_im1 + j] + in[base_ip1 + j] +\n                                 in[base_i + j - 1] + in[base_i + j + 1]);\n            out[base_j + 1] = 0.25 * (in[base_im1 + j + 1] + in[base_ip1 + j + 1] +\n                                     in[base_i + j] + in[base_i + j + 2]);\n            out[base_j + 2] = 0.25 * (in[base_im1 + j + 2] + in[base_ip1 + j + 2] +\n                                     in[base_i + j + 1] + in[base_i + j + 3]);\n            out[base_j + 3] = 0.25 * (in[base_im1 + j + 3] + in[base_ip1 + j + 3] +\n                                     in[base_i + j + 2] + in[base_i + j + 4]);\n        }\n        \n        // Handle remaining elements\n        for (; j <= Nm2; ++j) {\n            const int base_j = base_i + j;\n            out[base_j] = 0.25 * (in[base_im1 + j] + in[base_ip1 + j] +\n                                 in[base_i + j - 1] + in[base_i + j + 1]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic double GridChecksum(const std::vector<double> &grid, int N) {\n    double sum = 0.0;\n    for (int i = 0; i < N * N; ++i) sum += grid[i];\n    return sum;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int N = GRID_N;\n    int timesteps = 100;\n    int rounds = 5;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--grid\" && i + 1 < argc) N = std::atoi(argv[++i]);\n        else if (arg == \"--timesteps\" && i + 1 < argc) timesteps = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    // Initialize grid with deterministic values; boundaries stay 0\n    std::mt19937 rng(seed);\n    std::uniform_real_distribution<double> dist(0.0, 1.0);\n    std::vector<double> grid_a(N * N, 0.0);\n    std::vector<double> grid_b(N * N, 0.0);\n    for (int i = 1; i < N - 1; ++i)\n        for (int j = 1; j < N - 1; ++j)\n            grid_a[i * N + j] = dist(rng);\n\n    // Reference grids for correctness check (run 10 steps)\n    std::vector<double> ref_a(grid_a), ref_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        ReferenceStep(ref_a, ref_b, N);\n        std::swap(ref_a, ref_b);\n    }\n    double ref_checksum = GridChecksum(ref_a, N);\n\n    // Evolved function: same 10 steps\n    std::vector<double> test_a(grid_a), test_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        StencilStep(test_a, test_b, N);\n        std::swap(test_a, test_b);\n    }\n    double test_checksum = GridChecksum(test_a, N);\n\n    if (std::abs(ref_checksum - test_checksum) > 1e-6) {\n        std::cerr << \"stencil mismatch: ref=\" << ref_checksum\n                  << \" got=\" << test_checksum << std::endl;\n        return 2;\n    }\n\n    // Benchmark: each round runs `timesteps` stencil steps\n    std::vector<double> latencies;\n    latencies.reserve(rounds * timesteps);\n    double total_time = 0.0;\n    long long total_steps = 0;\n\n    for (int r = 0; r < rounds; ++r) {\n        // Re-init grid each round for consistent measurement\n        std::copy(grid_a.begin(), grid_a.end(), test_a.begin());\n        std::fill(test_b.begin(), test_b.end(), 0.0);\n\n        for (int t = 0; t < timesteps; ++t) {\n            auto t0 = std::chrono::high_resolution_clock::now();\n            StencilStep(test_a, test_b, N);\n            auto t1 = std::chrono::high_resolution_clock::now();\n            double dt = std::chrono::duration<double>(t1 - t0).count();\n            latencies.push_back(dt);\n            total_time += dt;\n            ++total_steps;\n            std::swap(test_a, test_b);\n        }\n    }\n\n    double ops_per_sec = total_steps / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n    // Each step updates (N-2)*(N-2) cells, 5 FLOP each\n    double gflops = (double)(N - 2) * (N - 2) * 5.0 * total_steps\n                  / total_time / 1e9;\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"gflops\\\":\" << gflops\n       << \",\\\"grid_size\\\":\" << N << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}\n```\nKey features: Performs well on combined_score (1.0394), Performs well on ops_per_sec (1657.7433), Performs well on p99_latency_us (610.4780), Performs well on bcoz_max_speedup (5.0000), Performs well on bperf_offcpu_ratio (0.0200), Performs well on gflops (8.6574)\n\n### Program 3 (Score: 1.0359)\n```cpp\n#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\nstatic const int GRID_N = 1024;\n\n// Reference stencil step (outside EVOLVE-BLOCK)\nstatic void ReferenceStep(const std::vector<double> &in,\n                          std::vector<double> &out, int N) {\n    for (int i = 1; i < N - 1; ++i) {\n        for (int j = 1; j < N - 1; ++j) {\n            out[i * N + j] = 0.25 * (in[(i - 1) * N + j] + in[(i + 1) * N + j]\n                                   + in[i * N + (j - 1)] + in[i * N + (j + 1)]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void StencilStep(const std::vector<double> &in,\n                        std::vector<double> &out, int N) {\n    // 5-point Jacobi stencil: average of 4 neighbors\n    // Highly optimized version with loop unrolling and cache-friendly access\n    const int Nm2 = N - 2;\n    \n    // Precompute base offsets to avoid repeated multiplication\n    for (int i = 1; i <= Nm2; ++i) {\n        const int base_i = i * N;\n        const int base_im1 = base_i - N;  // (i-1) * N\n        const int base_ip1 = base_i + N;  // (i+1) * N\n        \n        // Process 4 elements at a time for better ILP\n        int j = 1;\n        const int j_end = Nm2 - 3;\n        for (; j <= j_end; j += 4) {\n            const int base_j = base_i + j;\n            \n            // Compute four stencil points simultaneously\n            out[base_j] = 0.25 * (in[base_im1 + j] + in[base_ip1 + j] +\n                                 in[base_i + j - 1] + in[base_i + j + 1]);\n            out[base_j + 1] = 0.25 * (in[base_im1 + j + 1] + in[base_ip1 + j + 1] +\n                                     in[base_i + j] + in[base_i + j + 2]);\n            out[base_j + 2] = 0.25 * (in[base_im1 + j + 2] + in[base_ip1 + j + 2] +\n                                     in[base_i + j + 1] + in[base_i + j + 3]);\n            out[base_j + 3] = 0.25 * (in[base_im1 + j + 3] + in[base_ip1 + j + 3] +\n                                     in[base_i + j + 2] + in[base_i + j + 4]);\n        }\n        \n        // Handle remaining elements\n        for (; j <= Nm2; ++j) {\n            const int base_j = base_i + j;\n            out[base_j] = 0.25 * (in[base_im1 + j] + in[base_ip1 + j] +\n                                 in[base_i + j - 1] + in[base_i + j + 1]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic double GridChecksum(const std::vector<double> &grid, int N) {\n    double sum = 0.0;\n    for (int i = 0; i < N * N; ++i) sum += grid[i];\n    return sum;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int N = GRID_N;\n    int timesteps = 100;\n    int rounds = 5;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--grid\" && i + 1 < argc) N = std::atoi(argv[++i]);\n        else if (arg == \"--timesteps\" && i + 1 < argc) timesteps = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    // Initialize grid with deterministic values; boundaries stay 0\n    std::mt19937 rng(seed);\n    std::uniform_real_distribution<double> dist(0.0, 1.0);\n    std::vector<double> grid_a(N * N, 0.0);\n    std::vector<double> grid_b(N * N, 0.0);\n    for (int i = 1; i < N - 1; ++i)\n        for (int j = 1; j < N - 1; ++j)\n            grid_a[i * N + j] = dist(rng);\n\n    // Reference grids for correctness check (run 10 steps)\n    std::vector<double> ref_a(grid_a), ref_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        ReferenceStep(ref_a, ref_b, N);\n        std::swap(ref_a, ref_b);\n    }\n    double ref_checksum = GridChecksum(ref_a, N);\n\n    // Evolved function: same 10 steps\n    std::vector<double> test_a(grid_a), test_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        StencilStep(test_a, test_b, N);\n        std::swap(test_a, test_b);\n    }\n    double test_checksum = GridChecksum(test_a, N);\n\n    if (std::abs(ref_checksum - test_checksum) > 1e-6) {\n        std::cerr << \"stencil mismatch: ref=\" << ref_checksum\n                  << \" got=\" << test_checksum << std::endl;\n        return 2;\n    }\n\n    // Benchmark: each round runs `timesteps` stencil steps\n    std::vector<double> latencies;\n    latencies.reserve(rounds * timesteps);\n    double total_time = 0.0;\n    long long total_steps = 0;\n\n    for (int r = 0; r < rounds; ++r) {\n        // Re-init grid each round for consistent measurement\n        std::copy(grid_a.begin(), grid_a.end(), test_a.begin());\n        std::fill(test_b.begin(), test_b.end(), 0.0);\n\n        for (int t = 0; t < timesteps; ++t) {\n            auto t0 = std::chrono::high_resolution_clock::now();\n            StencilStep(test_a, test_b, N);\n            auto t1 = std::chrono::high_resolution_clock::now();\n            double dt = std::chrono::duration<double>(t1 - t0).count();\n            latencies.push_back(dt);\n            total_time += dt;\n            ++total_steps;\n            std::swap(test_a, test_b);\n        }\n    }\n\n    double ops_per_sec = total_steps / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n    // Each step updates (N-2)*(N-2) cells, 5 FLOP each\n    double gflops = (double)(N - 2) * (N - 2) * 5.0 * total_steps\n                  / total_time / 1e9;\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"gflops\\\":\" << gflops\n       << \",\\\"grid_size\\\":\" << N << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}\n```\nKey features: Performs well on combined_score (1.0359), Performs well on ops_per_sec (1658.4638), Performs well on p99_latency_us (618.0710), Performs well on bcoz_max_speedup (5.0000), Performs well on bperf_offcpu_ratio (0.0200), Performs well on gflops (8.6612)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.9866)\n```cpp\n#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\nstatic const int GRID_N = 1024;\n\n// Reference stencil step (outside EVOLVE-BLOCK)\nstatic void ReferenceStep(const std::vector<double> &in,\n                          std::vector<double> &out, int N) {\n    for (int i = 1; i < N - 1; ++i) {\n        for (int j = 1; j < N - 1; ++j) {\n            out[i * N + j] = 0.25 * (in[(i - 1) * N + j] + in[(i + 1) * N + j]\n                                   + in[i * N + (j - 1)] + in[i * N + (j + 1)]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void StencilStep(const std::vector<double> &in,\n                        std::vector<double> &out, int N) {\n    // 5-point Jacobi stencil: average of 4 neighbors\n    // Highly optimized version with loop unrolling and cache-friendly access\n    const int Nm2 = N - 2;\n    \n    // Process rows in chunks to improve cache locality\n    for (int i = 1; i <= Nm2; ++i) {\n        const int base_i = i * N;\n        const int base_im1 = (i - 1) * N;\n        const int base_ip1 = (i + 1) * N;\n        \n        // Unroll inner loop by 4 for better instruction-level parallelism\n        int j = 1;\n        for (; j <= Nm2 - 3; j += 4) {\n            const int base_j = base_i + j;\n            \n            // Compute four stencil points simultaneously\n            out[base_j] = 0.25 * (in[base_im1 + j] + in[base_ip1 + j] +\n                                 in[base_i + j - 1] + in[base_i + j + 1]);\n            out[base_j + 1] = 0.25 * (in[base_im1 + j + 1] + in[base_ip1 + j + 1] +\n                                     in[base_i + j] + in[base_i + j + 2]);\n            out[base_j + 2] = 0.25 * (in[base_im1 + j + 2] + in[base_ip1 + j + 2] +\n                                     in[base_i + j + 1] + in[base_i + j + 3]);\n            out[base_j + 3] = 0.25 * (in[base_im1 + j + 3] + in[base_ip1 + j + 3] +\n                                     in[base_i + j + 2] + in[base_i + j + 4]);\n        }\n        \n        // Handle remaining elements\n        for (; j <= Nm2; ++j) {\n            const int base_j = base_i + j;\n            out[base_j] = 0.25 * (in[base_im1 + j] + in[base_ip1 + j] +\n                                 in[base_i + j - 1] + in[base_i + j + 1]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic double GridChecksum(const std::vector<double> &grid, int N) {\n    double sum = 0.0;\n    for (int i = 0; i < N * N; ++i) sum += grid[i];\n    return sum;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int N = GRID_N;\n    int timesteps = 100;\n    int rounds = 5;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--grid\" && i + 1 < argc) N = std::atoi(argv[++i]);\n        else if (arg == \"--timesteps\" && i + 1 < argc) timesteps = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    // Initialize grid with deterministic values; boundaries stay 0\n    std::mt19937 rng(seed);\n    std::uniform_real_distribution<double> dist(0.0, 1.0);\n    std::vector<double> grid_a(N * N, 0.0);\n    std::vector<double> grid_b(N * N, 0.0);\n    for (int i = 1; i < N - 1; ++i)\n        for (int j = 1; j < N - 1; ++j)\n            grid_a[i * N + j] = dist(rng);\n\n    // Reference grids for correctness check (run 10 steps)\n    std::vector<double> ref_a(grid_a), ref_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        ReferenceStep(ref_a, ref_b, N);\n        std::swap(ref_a, ref_b);\n    }\n    double ref_checksum = GridChecksum(ref_a, N);\n\n    // Evolved function: same 10 steps\n    std::vector<double> test_a(grid_a), test_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        StencilStep(test_a, test_b, N);\n        std::swap(test_a, test_b);\n    }\n    double test_checksum = GridChecksum(test_a, N);\n\n    if (std::abs(ref_checksum - test_checksum) > 1e-6) {\n        std::cerr << \"stencil mismatch: ref=\" << ref_checksum\n                  << \" got=\" << test_checksum << std::endl;\n        return 2;\n    }\n\n    // Benchmark: each round runs `timesteps` stencil steps\n    std::vector<double> latencies;\n    latencies.reserve(rounds * timesteps);\n    double total_time = 0.0;\n    long long total_steps = 0;\n\n    for (int r = 0; r < rounds; ++r) {\n        // Re-init grid each round for consistent measurement\n        std::copy(grid_a.begin(), grid_a.end(), test_a.begin());\n        std::fill(test_b.begin(), test_b.end(), 0.0);\n\n        for (int t = 0; t < timesteps; ++t) {\n            auto t0 = std::chrono::high_resolution_clock::now();\n            StencilStep(test_a, test_b, N);\n            auto t1 = std::chrono::high_resolution_clock::now();\n            double dt = std::chrono::duration<double>(t1 - t0).count();\n            latencies.push_back(dt);\n            total_time += dt;\n            ++total_steps;\n            std::swap(test_a, test_b);\n        }\n    }\n\n    double ops_per_sec = total_steps / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n    // Each step updates (N-2)*(N-2) cells, 5 FLOP each\n    double gflops = (double)(N - 2) * (N - 2) * 5.0 * total_steps\n                  / total_time / 1e9;\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"gflops\\\":\" << gflops\n       << \",\\\"grid_size\\\":\" << N << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}\n```\nKey features: Alternative approach to combined_score, Alternative approach to ops_per_sec\n\n### Program D2 (Score: 1.0339)\n```cpp\n#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\nstatic const int GRID_N = 1024;\n\n// Reference stencil step (outside EVOLVE-BLOCK)\nstatic void ReferenceStep(const std::vector<double> &in,\n                          std::vector<double> &out, int N) {\n    for (int i = 1; i < N - 1; ++i) {\n        for (int j = 1; j < N - 1; ++j) {\n            out[i * N + j] = 0.25 * (in[(i - 1) * N + j] + in[(i + 1) * N + j]\n                                   + in[i * N + (j - 1)] + in[i * N + (j + 1)]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void StencilStep(const std::vector<double> &in,\n                        std::vector<double> &out, int N) {\n    // 5-point Jacobi stencil: average of 4 neighbors\n    // Highly optimized version with minimal overhead\n    const int Nm2 = N - 2;\n    \n    // Process rows sequentially for cache efficiency\n    for (int i = 1; i <= Nm2; ++i) {\n        const int base_i = i * N;\n        const int base_im1 = (i - 1) * N;\n        const int base_ip1 = (i + 1) * N;\n        \n        // Unroll inner loop by 4 for better instruction-level parallelism\n        int j = 1;\n        const int j_end = Nm2 - 3;\n        for (; j <= j_end; j += 4) {\n            const int base_j = base_i + j;\n            \n            // Compute four stencil points simultaneously\n            out[base_j] = 0.25 * (in[base_im1 + j] + in[base_ip1 + j] +\n                                 in[base_i + j - 1] + in[base_i + j + 1]);\n            out[base_j + 1] = 0.25 * (in[base_im1 + j + 1] + in[base_ip1 + j + 1] +\n                                     in[base_i + j] + in[base_i + j + 2]);\n            out[base_j + 2] = 0.25 * (in[base_im1 + j + 2] + in[base_ip1 + j + 2] +\n                                     in[base_i + j + 1] + in[base_i + j + 3]);\n            out[base_j + 3] = 0.25 * (in[base_im1 + j + 3] + in[base_ip1 + j + 3] +\n                                     in[base_i + j + 2] + in[base_i + j + 4]);\n        }\n        \n        // Handle remaining elements\n        for (; j <= Nm2; ++j) {\n            const int base_j = base_i + j;\n            out[base_j] = 0.25 * (in[base_im1 + j] + in[base_ip1 + j] +\n                                 in[base_i + j - 1] + in[base_i + j + 1]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic double GridChecksum(const std::vector<double> &grid, int N) {\n    double sum = 0.0;\n    for (int i = 0; i < N * N; ++i) sum += grid[i];\n    return sum;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int N = GRID_N;\n    int timesteps = 100;\n    int rounds = 5;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--grid\" && i + 1 < argc) N = std::atoi(argv[++i]);\n        else if (arg == \"--timesteps\" && i + 1 < argc) timesteps = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    // Initialize grid with deterministic values; boundaries stay 0\n    std::mt19937 rng(seed);\n    std::uniform_real_distribution<double> dist(0.0, 1.0);\n    std::vector<double> grid_a(N * N, 0.0);\n    std::vector<double> grid_b(N * N, 0.0);\n    for (int i = 1; i < N - 1; ++i)\n        for (int j = 1; j < N - 1; ++j)\n            grid_a[i * N + j] = dist(rng);\n\n    // Reference grids for correctness check (run 10 steps)\n    std::vector<double> ref_a(grid_a), ref_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        ReferenceStep(ref_a, ref_b, N);\n        std::swap(ref_a, ref_b);\n    }\n    double ref_checksum = GridChecksum(ref_a, N);\n\n    // Evolved function: same 10 steps\n    std::vector<double> test_a(grid_a), test_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        StencilStep(test_a, test_b, N);\n        std::swap(test_a, test_b);\n    }\n    double test_checksum = GridChecksum(test_a, N);\n\n    if (std::abs(ref_checksum - test_checksum) > 1e-6) {\n        std::cerr << \"stencil mismatch: ref=\" << ref_checksum\n                  << \" got=\" << test_checksum << std::endl;\n        return 2;\n    }\n\n    // Benchmark: each round runs `timesteps` stencil steps\n    std::vector<double> latencies;\n    latencies.reserve(rounds * timesteps);\n    double total_time = 0.0;\n    long long total_steps = 0;\n\n    for (int r = 0; r < rounds; ++r) {\n        // Re-init grid each round for consistent measurement\n        std::copy(grid_a.begin(), grid_a.end(), test_a.begin());\n        std::fill(test_b.begin(), test_b.end(), 0.0);\n\n        for (int t = 0; t < timesteps; ++t) {\n            auto t0 = std::chrono::high_resolution_clock::now();\n            StencilStep(test_a, test_b, N);\n            auto t1 = std::chrono::high_resolution_clock::now();\n            double dt = std::chrono::duration<double>(t1 - t0).count();\n            latencies.push_back(dt);\n            total_time += dt;\n            ++total_steps;\n            std::swap(test_a, test_b);\n        }\n    }\n\n    double ops_per_sec = total_steps / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n    // Each step updates (N-2)*(N-2) cells, 5 FLOP each\n    double gflops = (double)(N - 2) * (N - 2) * 5.0 * total_steps\n                  / total_time / 1e9;\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"gflops\\\":\" << gflops\n       << \",\\\"grid_size\\\":\" << N << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}\n```\nKey features: Alternative approach to combined_score, Alternative approach to ops_per_sec\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.9866, Type: High-Performer)\n```cpp\n#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\nstatic const int GRID_N = 1024;\n\n// Reference stencil step (outside EVOLVE-BLOCK)\nstatic void ReferenceStep(const std::vector<double> &in,\n                          std::vector<double> &out, int N) {\n    for (int i = 1; i < N - 1; ++i) {\n        for (int j = 1; j < N - 1; ++j) {\n            out[i * N + j] = 0.25 * (in[(i - 1) * N + j] + in[(i + 1) * N + j]\n                                   + in[i * N + (j - 1)] + in[i * N + (j + 1)]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void StencilStep(const std::vector<double> &in,\n                        std::vector<double> &out, int N) {\n    // 5-point Jacobi stencil: average of 4 neighbors\n    // Highly optimized version with loop unrolling and cache-friendly access\n    const int Nm2 = N - 2;\n    \n    // Process rows in chunks to improve cache locality\n    for (int i = 1; i <= Nm2; ++i) {\n        const int base_i = i * N;\n        const int base_im1 = (i - 1) * N;\n        const int base_ip1 = (i + 1) * N;\n        \n        // Unroll inner loop by 4 for better instruction-level parallelism\n        int j = 1;\n        for (; j <= Nm2 - 3; j += 4) {\n            const int base_j = base_i + j;\n            \n            // Compute four stencil points simultaneously\n            out[base_j] = 0.25 * (in[base_im1 + j] + in[base_ip1 + j] +\n                                 in[base_i + j - 1] + in[base_i + j + 1]);\n            out[base_j + 1] = 0.25 * (in[base_im1 + j + 1] + in[base_ip1 + j + 1] +\n                                     in[base_i + j] + in[base_i + j + 2]);\n            out[base_j + 2] = 0.25 * (in[base_im1 + j + 2] + in[base_ip1 + j + 2] +\n                                     in[base_i + j + 1] + in[base_i + j + 3]);\n            out[base_j + 3] = 0.25 * (in[base_im1 + j + 3] + in[base_ip1 + j + 3] +\n                                     in[base_i + j + 2] + in[base_i + j + 4]);\n        }\n        \n        // Handle remaining elements\n        for (; j <= Nm2; ++j) {\n            const int base_j = base_i + j;\n            out[base_j] = 0.25 * (in[base_im1 + j] + in[base_ip1 + j] +\n                                 in[base_i + j - 1] + in[base_i + j + 1]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic double GridChecksum(const std::vector<double> &grid, int N) {\n    double sum = 0.0;\n    for (int i = 0; i < N * N; ++i) sum += grid[i];\n    return sum;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int N = GRID_N;\n    int timesteps = 100;\n    int rounds = 5;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--grid\" && i + 1 < argc) N = std::atoi(argv[++i]);\n        else if (arg == \"--timesteps\" && i + 1 < argc) timesteps = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    // Initialize grid with deterministic values; boundaries stay 0\n    std::mt19937 rng(seed);\n    std::uniform_real_distribution<double> dist(0.0, 1.0);\n    std::vector<double> grid_a(N * N, 0.0);\n    std::vector<double> grid_b(N * N, 0.0);\n    for (int i = 1; i < N - 1; ++i)\n        for (int j = 1; j < N - 1; ++j)\n            grid_a[i * N + j] = dist(rng);\n\n    // Reference grids for correctness check (run 10 steps)\n    std::vector<double> ref_a(grid_a), ref_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        ReferenceStep(ref_a, ref_b, N);\n        std::swap(ref_a, ref_b);\n    }\n    double ref_checksum = GridChecksum(ref_a, N);\n\n    // Evolved function: same 10 steps\n    std::vector<double> test_a(grid_a), test_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        StencilStep(test_a, test_b, N);\n        std::swap(test_a, test_b);\n    }\n    double test_checksum = GridChecksum(test_a, N);\n\n    if (std::abs(ref_checksum - test_checksum) > 1e-6) {\n        std::cerr << \"stencil mismatch: ref=\" << ref_checksum\n                  << \" got=\" << test_checksum << std::endl;\n        return 2;\n    }\n\n    // Benchmark: each round runs `timesteps` stencil steps\n    std::vector<double> latencies;\n    latencies.reserve(rounds * timesteps);\n    double total_time = 0.0;\n    long long total_steps = 0;\n\n    for (int r = 0; r < rounds; ++r) {\n        // Re-init grid each round for consistent measurement\n        std::copy(grid_a.begin(), grid_a.end(), test_a.begin());\n        std::fill(test_b.begin(), test_b.end(), 0.0);\n\n        for (int t = 0; t < timesteps; ++t) {\n            auto t0 = std::chrono::high_resolution_clock::now();\n            StencilStep(test_a, test_b, N);\n            auto t1 = std::chrono::high_resolution_clock::now();\n            double dt = std::chrono::duration<double>(t1 - t0).count();\n            latencies.push_back(dt);\n            total_time += dt;\n            ++total_steps;\n            std::swap(test_a, test_b);\n        }\n    }\n\n    double ops_per_sec = total_steps / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n    // Each step updates (N-2)*(N-2) cells, 5 FLOP each\n    double gflops = (double)(N - 2) * (N - 2) * 5.0 * total_steps\n                  / total_time / 1e9;\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"gflops\\\":\" << gflops\n       << \",\\\"grid_size\\\":\" << N << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 1.0572, Type: High-Performer)\n```cpp\n#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\nstatic const int GRID_N = 1024;\n\n// Reference stencil step (outside EVOLVE-BLOCK)\nstatic void ReferenceStep(const std::vector<double> &in,\n                          std::vector<double> &out, int N) {\n    for (int i = 1; i < N - 1; ++i) {\n        for (int j = 1; j < N - 1; ++j) {\n            out[i * N + j] = 0.25 * (in[(i - 1) * N + j] + in[(i + 1) * N + j]\n                                   + in[i * N + (j - 1)] + in[i * N + (j + 1)]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void StencilStep(const std::vector<double> &in,\n                        std::vector<double> &out, int N) {\n    // 5-point Jacobi stencil: average of 4 neighbors\n    // Optimized version with loop unrolling and reduced memory access\n    const int Nm2 = N - 2;\n    \n    // Process rows in chunks to improve cache locality\n    for (int i = 1; i <= Nm2; ++i) {\n        const int base_i = i * N;\n        const int base_im1 = (i - 1) * N;\n        const int base_ip1 = (i + 1) * N;\n        \n        // Unroll inner loop by 4 for better instruction-level parallelism\n        int j = 1;\n        for (; j <= Nm2 - 3; j += 4) {\n            const int base_j = base_i + j;\n            \n            // Compute four stencil points simultaneously\n            out[base_j] = 0.25 * (in[base_im1 + j] + in[base_ip1 + j] +\n                                 in[base_i + j - 1] + in[base_i + j + 1]);\n            out[base_j + 1] = 0.25 * (in[base_im1 + j + 1] + in[base_ip1 + j + 1] +\n                                     in[base_i + j] + in[base_i + j + 2]);\n            out[base_j + 2] = 0.25 * (in[base_im1 + j + 2] + in[base_ip1 + j + 2] +\n                                     in[base_i + j + 1] + in[base_i + j + 3]);\n            out[base_j + 3] = 0.25 * (in[base_im1 + j + 3] + in[base_ip1 + j + 3] +\n                                     in[base_i + j + 2] + in[base_i + j + 4]);\n        }\n        \n        // Handle remaining elements\n        for (; j <= Nm2; ++j) {\n            const int base_j = base_i + j;\n            out[base_j] = 0.25 * (in[base_im1 + j] + in[base_ip1 + j] +\n                                 in[base_i + j - 1] + in[base_i + j + 1]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic double GridChecksum(const std::vector<double> &grid, int N) {\n    double sum = 0.0;\n    for (int i = 0; i < N * N; ++i) sum += grid[i];\n    return sum;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int N = GRID_N;\n    int timesteps = 100;\n    int rounds = 5;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--grid\" && i + 1 < argc) N = std::atoi(argv[++i]);\n        else if (arg == \"--timesteps\" && i + 1 < argc) timesteps = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    // Initialize grid with deterministic values; boundaries stay 0\n    std::mt19937 rng(seed);\n    std::uniform_real_distribution<double> dist(0.0, 1.0);\n    std::vector<double> grid_a(N * N, 0.0);\n    std::vector<double> grid_b(N * N, 0.0);\n    for (int i = 1; i < N - 1; ++i)\n        for (int j = 1; j < N - 1; ++j)\n            grid_a[i * N + j] = dist(rng);\n\n    // Reference grids for correctness check (run 10 steps)\n    std::vector<double> ref_a(grid_a), ref_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        ReferenceStep(ref_a, ref_b, N);\n        std::swap(ref_a, ref_b);\n    }\n    double ref_checksum = GridChecksum(ref_a, N);\n\n    // Evolved function: same 10 steps\n    std::vector<double> test_a(grid_a), test_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        StencilStep(test_a, test_b, N);\n        std::swap(test_a, test_b);\n    }\n    double test_checksum = GridChecksum(test_a, N);\n\n    if (std::abs(ref_checksum - test_checksum) > 1e-6) {\n        std::cerr << \"stencil mismatch: ref=\" << ref_checksum\n                  << \" got=\" << test_checksum << std::endl;\n        return 2;\n    }\n\n    // Benchmark: each round runs `timesteps` stencil steps\n    std::vector<double> latencies;\n    latencies.reserve(rounds * timesteps);\n    double total_time = 0.0;\n    long long total_steps = 0;\n\n    for (int r = 0; r < rounds; ++r) {\n        // Re-init grid each round for consistent measurement\n        std::copy(grid_a.begin(), grid_a.end(), test_a.begin());\n        std::fill(test_b.begin(), test_b.end(), 0.0);\n\n        for (int t = 0; t < timesteps; ++t) {\n            auto t0 = std::chrono::high_resolution_clock::now();\n            StencilStep(test_a, test_b, N);\n            auto t1 = std::chrono::high_resolution_clock::now();\n            double dt = std::chrono::duration<double>(t1 - t0).count();\n            latencies.push_back(dt);\n            total_time += dt;\n            ++total_steps;\n            std::swap(test_a, test_b);\n        }\n    }\n\n    double ops_per_sec = total_steps / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n    // Each step updates (N-2)*(N-2) cells, 5 FLOP each\n    double gflops = (double)(N - 2) * (N - 2) * 5.0 * total_steps\n                  / total_time / 1e9;\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"gflops\\\":\" << gflops\n       << \",\\\"grid_size\\\":\" << N << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 1.0394, Type: High-Performer)\n```cpp\n#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\nstatic const int GRID_N = 1024;\n\n// Reference stencil step (outside EVOLVE-BLOCK)\nstatic void ReferenceStep(const std::vector<double> &in,\n                          std::vector<double> &out, int N) {\n    for (int i = 1; i < N - 1; ++i) {\n        for (int j = 1; j < N - 1; ++j) {\n            out[i * N + j] = 0.25 * (in[(i - 1) * N + j] + in[(i + 1) * N + j]\n                                   + in[i * N + (j - 1)] + in[i * N + (j + 1)]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void StencilStep(const std::vector<double> &in,\n                        std::vector<double> &out, int N) {\n    // 5-point Jacobi stencil: average of 4 neighbors\n    // Highly optimized version with loop unrolling and cache-friendly access\n    const int Nm2 = N - 2;\n    \n    // Process rows in chunks to improve cache locality\n    for (int i = 1; i <= Nm2; ++i) {\n        const int base_i = i * N;\n        const int base_im1 = (i - 1) * N;\n        const int base_ip1 = (i + 1) * N;\n        \n        // Unroll inner loop by 4 for better instruction-level parallelism\n        int j = 1;\n        for (; j <= Nm2 - 3; j += 4) {\n            const int base_j = base_i + j;\n            \n            // Compute four stencil points simultaneously\n            out[base_j] = 0.25 * (in[base_im1 + j] + in[base_ip1 + j] +\n                                 in[base_i + j - 1] + in[base_i + j + 1]);\n            out[base_j + 1] = 0.25 * (in[base_im1 + j + 1] + in[base_ip1 + j + 1] +\n                                     in[base_i + j] + in[base_i + j + 2]);\n            out[base_j + 2] = 0.25 * (in[base_im1 + j + 2] + in[base_ip1 + j + 2] +\n                                     in[base_i + j + 1] + in[base_i + j + 3]);\n            out[base_j + 3] = 0.25 * (in[base_im1 + j + 3] + in[base_ip1 + j + 3] +\n                                     in[base_i + j + 2] + in[base_i + j + 4]);\n        }\n        \n        // Handle remaining elements\n        for (; j <= Nm2; ++j) {\n            const int base_j = base_i + j;\n            out[base_j] = 0.25 * (in[base_im1 + j] + in[base_ip1 + j] +\n                                 in[base_i + j - 1] + in[base_i + j + 1]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic double GridChecksum(const std::vector<double> &grid, int N) {\n    double sum = 0.0;\n    for (int i = 0; i < N * N; ++i) sum += grid[i];\n    return sum;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int N = GRID_N;\n    int timesteps = 100;\n    int rounds = 5;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--grid\" && i + 1 < argc) N = std::atoi(argv[++i]);\n        else if (arg == \"--timesteps\" && i + 1 < argc) timesteps = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    // Initialize grid with deterministic values; boundaries stay 0\n    std::mt19937 rng(seed);\n    std::uniform_real_distribution<double> dist(0.0, 1.0);\n    std::vector<double> grid_a(N * N, 0.0);\n    std::vector<double> grid_b(N * N, 0.0);\n    for (int i = 1; i < N - 1; ++i)\n        for (int j = 1; j < N - 1; ++j)\n            grid_a[i * N + j] = dist(rng);\n\n    // Reference grids for correctness check (run 10 steps)\n    std::vector<double> ref_a(grid_a), ref_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        ReferenceStep(ref_a, ref_b, N);\n        std::swap(ref_a, ref_b);\n    }\n    double ref_checksum = GridChecksum(ref_a, N);\n\n    // Evolved function: same 10 steps\n    std::vector<double> test_a(grid_a), test_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        StencilStep(test_a, test_b, N);\n        std::swap(test_a, test_b);\n    }\n    double test_checksum = GridChecksum(test_a, N);\n\n    if (std::abs(ref_checksum - test_checksum) > 1e-6) {\n        std::cerr << \"stencil mismatch: ref=\" << ref_checksum\n                  << \" got=\" << test_checksum << std::endl;\n        return 2;\n    }\n\n    // Benchmark: each round runs `timesteps` stencil steps\n    std::vector<double> latencies;\n    latencies.reserve(rounds * timesteps);\n    double total_time = 0.0;\n    long long total_steps = 0;\n\n    for (int r = 0; r < rounds; ++r) {\n        // Re-init grid each round for consistent measurement\n        std::copy(grid_a.begin(), grid_a.end(), test_a.begin());\n        std::fill(test_b.begin(), test_b.end(), 0.0);\n\n        for (int t = 0; t < timesteps; ++t) {\n            auto t0 = std::chrono::high_resolution_clock::now();\n            StencilStep(test_a, test_b, N);\n            auto t1 = std::chrono::high_resolution_clock::now();\n            double dt = std::chrono::duration<double>(t1 - t0).count();\n            latencies.push_back(dt);\n            total_time += dt;\n            ++total_steps;\n            std::swap(test_a, test_b);\n        }\n    }\n\n    double ops_per_sec = total_steps / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n    // Each step updates (N-2)*(N-2) cells, 5 FLOP each\n    double gflops = (double)(N - 2) * (N - 2) * 5.0 * total_steps\n                  / total_time / 1e9;\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"gflops\\\":\" << gflops\n       << \",\\\"grid_size\\\":\" << N << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```cpp\n#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\nstatic const int GRID_N = 1024;\n\n// Reference stencil step (outside EVOLVE-BLOCK)\nstatic void ReferenceStep(const std::vector<double> &in,\n                          std::vector<double> &out, int N) {\n    for (int i = 1; i < N - 1; ++i) {\n        for (int j = 1; j < N - 1; ++j) {\n            out[i * N + j] = 0.25 * (in[(i - 1) * N + j] + in[(i + 1) * N + j]\n                                   + in[i * N + (j - 1)] + in[i * N + (j + 1)]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void StencilStep(const std::vector<double> &in,\n                        std::vector<double> &out, int N) {\n    // 5-point Jacobi stencil: average of 4 neighbors\n    // Highly optimized version with minimal overhead\n    const int Nm2 = N - 2;\n    \n    // Process rows sequentially for cache efficiency\n    for (int i = 1; i <= Nm2; ++i) {\n        const int base_i = i * N;\n        const int base_im1 = (i - 1) * N;\n        const int base_ip1 = (i + 1) * N;\n        \n        // Unroll inner loop by 4 for better instruction-level parallelism\n        int j = 1;\n        const int j_end = Nm2 - 3;\n        for (; j <= j_end; j += 4) {\n            const int base_j = base_i + j;\n            \n            // Compute four stencil points simultaneously\n            out[base_j] = 0.25 * (in[base_im1 + j] + in[base_ip1 + j] +\n                                 in[base_i + j - 1] + in[base_i + j + 1]);\n            out[base_j + 1] = 0.25 * (in[base_im1 + j + 1] + in[base_ip1 + j + 1] +\n                                     in[base_i + j] + in[base_i + j + 2]);\n            out[base_j + 2] = 0.25 * (in[base_im1 + j + 2] + in[base_ip1 + j + 2] +\n                                     in[base_i + j + 1] + in[base_i + j + 3]);\n            out[base_j + 3] = 0.25 * (in[base_im1 + j + 3] + in[base_ip1 + j + 3] +\n                                     in[base_i + j + 2] + in[base_i + j + 4]);\n        }\n        \n        // Handle remaining elements\n        for (; j <= Nm2; ++j) {\n            const int base_j = base_i + j;\n            out[base_j] = 0.25 * (in[base_im1 + j] + in[base_ip1 + j] +\n                                 in[base_i + j - 1] + in[base_i + j + 1]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic double GridChecksum(const std::vector<double> &grid, int N) {\n    double sum = 0.0;\n    for (int i = 0; i < N * N; ++i) sum += grid[i];\n    return sum;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int N = GRID_N;\n    int timesteps = 100;\n    int rounds = 5;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--grid\" && i + 1 < argc) N = std::atoi(argv[++i]);\n        else if (arg == \"--timesteps\" && i + 1 < argc) timesteps = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    // Initialize grid with deterministic values; boundaries stay 0\n    std::mt19937 rng(seed);\n    std::uniform_real_distribution<double> dist(0.0, 1.0);\n    std::vector<double> grid_a(N * N, 0.0);\n    std::vector<double> grid_b(N * N, 0.0);\n    for (int i = 1; i < N - 1; ++i)\n        for (int j = 1; j < N - 1; ++j)\n            grid_a[i * N + j] = dist(rng);\n\n    // Reference grids for correctness check (run 10 steps)\n    std::vector<double> ref_a(grid_a), ref_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        ReferenceStep(ref_a, ref_b, N);\n        std::swap(ref_a, ref_b);\n    }\n    double ref_checksum = GridChecksum(ref_a, N);\n\n    // Evolved function: same 10 steps\n    std::vector<double> test_a(grid_a), test_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        StencilStep(test_a, test_b, N);\n        std::swap(test_a, test_b);\n    }\n    double test_checksum = GridChecksum(test_a, N);\n\n    if (std::abs(ref_checksum - test_checksum) > 1e-6) {\n        std::cerr << \"stencil mismatch: ref=\" << ref_checksum\n                  << \" got=\" << test_checksum << std::endl;\n        return 2;\n    }\n\n    // Benchmark: each round runs `timesteps` stencil steps\n    std::vector<double> latencies;\n    latencies.reserve(rounds * timesteps);\n    double total_time = 0.0;\n    long long total_steps = 0;\n\n    for (int r = 0; r < rounds; ++r) {\n        // Re-init grid each round for consistent measurement\n        std::copy(grid_a.begin(), grid_a.end(), test_a.begin());\n        std::fill(test_b.begin(), test_b.end(), 0.0);\n\n        for (int t = 0; t < timesteps; ++t) {\n            auto t0 = std::chrono::high_resolution_clock::now();\n            StencilStep(test_a, test_b, N);\n            auto t1 = std::chrono::high_resolution_clock::now();\n            double dt = std::chrono::duration<double>(t1 - t0).count();\n            latencies.push_back(dt);\n            total_time += dt;\n            ++total_steps;\n            std::swap(test_a, test_b);\n        }\n    }\n\n    double ops_per_sec = total_steps / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n    // Each step updates (N-2)*(N-2) cells, 5 FLOP each\n    double gflops = (double)(N - 2) * (N - 2) * 5.0 * total_steps\n                  / total_time / 1e9;\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"gflops\\\":\" << gflops\n       << \",\\\"grid_size\\\":\" << N << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: ops_per_sec, p99_latency_us\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```cpp\n# Your rewritten program here\n```", "responses": ["Looking at the current program and the requirement to optimize the 2D stencil computation inside the EVOLVE-BLOCK, I need to create a more efficient implementation that maintains the same function signature and produces identical results.\n\nThe key improvements I'll make:\n1. Reduce memory access patterns by precomputing indices more efficiently\n2. Improve loop structure for better instruction-level parallelism\n3. Minimize redundant calculations\n4. Ensure optimal cache usage\n\nHere's my optimized implementation:\n\n```cpp\n#include <algorithm>\n#include <chrono>\n#include <cmath>\n#include <cstdlib>\n#include <fstream>\n#include <iostream>\n#include <random>\n#include <sstream>\n#include <string>\n#include <vector>\n\nstatic const int GRID_N = 1024;\n\n// Reference stencil step (outside EVOLVE-BLOCK)\nstatic void ReferenceStep(const std::vector<double> &in,\n                          std::vector<double> &out, int N) {\n    for (int i = 1; i < N - 1; ++i) {\n        for (int j = 1; j < N - 1; ++j) {\n            out[i * N + j] = 0.25 * (in[(i - 1) * N + j] + in[(i + 1) * N + j]\n                                   + in[i * N + (j - 1)] + in[i * N + (j + 1)]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-START\n\nstatic void StencilStep(const std::vector<double> &in,\n                        std::vector<double> &out, int N) {\n    // 5-point Jacobi stencil: average of 4 neighbors\n    // Highly optimized version with minimal overhead and improved cache locality\n    const int Nm2 = N - 2;\n    \n    // Process rows sequentially with optimized loop structure\n    for (int i = 1; i <= Nm2; ++i) {\n        const int base_i = i * N;\n        const int base_im1 = base_i - N;  // (i-1) * N\n        const int base_ip1 = base_i + N;  // (i+1) * N\n        \n        // Unroll inner loop by 4 for better instruction-level parallelism\n        int j = 1;\n        const int j_end = Nm2 - 3;\n        for (; j <= j_end; j += 4) {\n            const int base_j = base_i + j;\n            \n            // Compute four stencil points simultaneously using precomputed offsets\n            const double val0 = in[base_im1 + j] + in[base_ip1 + j] +\n                               in[base_i + j - 1] + in[base_i + j + 1];\n            const double val1 = in[base_im1 + j + 1] + in[base_ip1 + j + 1] +\n                               in[base_i + j] + in[base_i + j + 2];\n            const double val2 = in[base_im1 + j + 2] + in[base_ip1 + j + 2] +\n                               in[base_i + j + 1] + in[base_i + j + 3];\n            const double val3 = in[base_im1 + j + 3] + in[base_ip1 + j + 3] +\n                               in[base_i + j + 2] + in[base_i + j + 4];\n            \n            out[base_j] = 0.25 * val0;\n            out[base_j + 1] = 0.25 * val1;\n            out[base_j + 2] = 0.25 * val2;\n            out[base_j + 3] = 0.25 * val3;\n        }\n        \n        // Handle remaining elements\n        for (; j <= Nm2; ++j) {\n            const int base_j = base_i + j;\n            out[base_j] = 0.25 * (in[base_im1 + j] + in[base_ip1 + j] +\n                                 in[base_i + j - 1] + in[base_i + j + 1]);\n        }\n    }\n}\n\n// EVOLVE-BLOCK-END\n\nstatic double GridChecksum(const std::vector<double> &grid, int N) {\n    double sum = 0.0;\n    for (int i = 0; i < N * N; ++i) sum += grid[i];\n    return sum;\n}\n\nstatic void WriteJson(const std::string &path, const std::string &payload) {\n    if (path.empty()) { std::cout << payload << std::endl; return; }\n    std::ofstream out(path.c_str(), std::ios::binary);\n    out << payload;\n}\n\nint main(int argc, char **argv) {\n    std::string json_path;\n    int seed = 42;\n    int N = GRID_N;\n    int timesteps = 100;\n    int rounds = 5;\n\n    for (int i = 1; i < argc; ++i) {\n        std::string arg = argv[i];\n        if (arg == \"--json\" && i + 1 < argc) json_path = argv[++i];\n        else if (arg == \"--seed\" && i + 1 < argc) seed = std::atoi(argv[++i]);\n        else if (arg == \"--grid\" && i + 1 < argc) N = std::atoi(argv[++i]);\n        else if (arg == \"--timesteps\" && i + 1 < argc) timesteps = std::atoi(argv[++i]);\n        else if (arg == \"--rounds\" && i + 1 < argc) rounds = std::atoi(argv[++i]);\n    }\n\n    // Initialize grid with deterministic values; boundaries stay 0\n    std::mt19937 rng(seed);\n    std::uniform_real_distribution<double> dist(0.0, 1.0);\n    std::vector<double> grid_a(N * N, 0.0);\n    std::vector<double> grid_b(N * N, 0.0);\n    for (int i = 1; i < N - 1; ++i)\n        for (int j = 1; j < N - 1; ++j)\n            grid_a[i * N + j] = dist(rng);\n\n    // Reference grids for correctness check (run 10 steps)\n    std::vector<double> ref_a(grid_a), ref_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        ReferenceStep(ref_a, ref_b, N);\n        std::swap(ref_a, ref_b);\n    }\n    double ref_checksum = GridChecksum(ref_a, N);\n\n    // Evolved function: same 10 steps\n    std::vector<double> test_a(grid_a), test_b(N * N, 0.0);\n    for (int t = 0; t < 10; ++t) {\n        StencilStep(test_a, test_b, N);\n        std::swap(test_a, test_b);\n    }\n    double test_checksum = GridChecksum(test_a, N);\n\n    if (std::abs(ref_checksum - test_checksum) > 1e-6) {\n        std::cerr << \"stencil mismatch: ref=\" << ref_checksum\n                  << \" got=\" << test_checksum << std::endl;\n        return 2;\n    }\n\n    // Benchmark: each round runs `timesteps` stencil steps\n    std::vector<double> latencies;\n    latencies.reserve(rounds * timesteps);\n    double total_time = 0.0;\n    long long total_steps = 0;\n\n    for (int r = 0; r < rounds; ++r) {\n        // Re-init grid each round for consistent measurement\n        std::copy(grid_a.begin(), grid_a.end(), test_a.begin());\n        std::fill(test_b.begin(), test_b.end(), 0.0);\n\n        for (int t = 0; t < timesteps; ++t) {\n            auto t0 = std::chrono::high_resolution_clock::now();\n            StencilStep(test_a, test_b, N);\n            auto t1 = std::chrono::high_resolution_clock::now();\n            double dt = std::chrono::duration<double>(t1 - t0).count();\n            latencies.push_back(dt);\n            total_time += dt;\n            ++total_steps;\n            std::swap(test_a, test_b);\n        }\n    }\n\n    double ops_per_sec = total_steps / total_time;\n    std::sort(latencies.begin(), latencies.end());\n    double p99 = latencies.empty() ? 0.0\n        : latencies[(size_t)(0.99 * (latencies.size() - 1))];\n    // Each step updates (N-2)*(N-2) cells, 5 FLOP each\n    double gflops = (double)(N - 2) * (N - 2) * 5.0 * total_steps\n                  / total_time / 1e9;\n\n    std::ostringstream os;\n    os.setf(std::ios::fixed);\n    os.precision(6);\n    os << \"{\\\"ops_per_sec\\\":\" << ops_per_sec\n       << \",\\\"p99_latency_us\\\":\" << (p99 * 1e6)\n       << \",\\\"gflops\\\":\" << gflops\n       << \",\\\"grid_size\\\":\" << N << \"}\";\n    WriteJson(json_path, os.str());\n    return 0;\n}\n```"]}}, "artifacts_json": null, "artifact_dir": null, "embedding": null}